# S02: Apply / refresh_max_dur / cleanse policy

**Goal:** Implement §H.1 status policy: multi-instance `StatusBag` storage per (target,kind), `refresh_max_dur` on re-apply of the same kind, and a `BuffKind`-classified cleanse that removes only Debuffs (Blessed survives). No per-status semantics (those are S03-S05), no new Effect-DSL variants (M019), no source attribution (M020).
**Demo:** Test deterministico: apply Heated(dur=2), re-apply Heated(dur=1), check dur=2. Cleanse rimuove Debuff ma non Buff cleanse-immune.

## Must-Haves

- `cargo check` + full `cargo test` headless suite green
- New `StatusBag` component holds N `StatusInstance`s per entity; `apply(kind,dur)` upserts with `max(old,new)` duration; never duplicates same-kind instances
- `BuffKind { Buff, Debuff }` with `classify_buff_kind(StatusEffectKind)` total over the 7 enum variants (Blessed=Buff; Heated/Chilled/Paralyzed/Slowed/Burn/Shock=Debuff)
- `StatusBag::cleanse_debuffs() -> Vec<StatusEffectKind>` removes every Debuff instance and leaves Buff (Blessed) intact, returning kinds removed
- `StatusEffectKind` enum and all `Effect`/event payloads carrying it stay unchanged; only the `StatusEffect` struct is replaced
- Bootstrap seeds every unit with empty `StatusBag::default()` alongside `RoundFlags::default()` (eliminates the `commands.insert` branch in the apply path)
- Apply pipeline (`turn_system/pipeline.rs:731`), **both** tick query sites (`turn_system/mod.rs:67` ResolveActorsQuery type alias AND `turn_system/mod.rs:369` advance_turn_system inline tuple), expire path (`turn_system/mod.rs:500`), and `follow_up.rs:104` query migrated to `StatusBag`; in-tree `turn_system/tests.rs` fixtures (lines 136, 155, 162, 183, 184, 199, 206, 234, 286, 287, 302, 324, 337-339) updated
- New deterministic integration tests: `tests/status_refresh_max_dur.rs`, `tests/status_multi_kind_coexist.rs`, `tests/status_cleanse_policy.rs` all green
- Legacy lifecycle tests `tests/status_effect_apply.rs`, `tests/status_effect_integration.rs`, `tests/status_effect_turn_tick.rs` deleted per M017-CONTEXT delete-and-rewrite-fresh directive (S01 drift reconciled); fresh `tests/status_accuracy.rs` written covering the accuracy gate; `tests/combat_coherence.rs` helper at line 410 migrated
- Tick emits one `OnStatusExpired { kind }` per expired instance; `OnStatusApplied` still fires on both insert and refresh; `OnStatusResisted` still gated by `roll_pct` (refresh-on-fail does NOT happen)
- Grep guard from S01 still clean — no `Burn|Freeze|Shock|DeepFreeze` regressions in src/ or tests/ beyond reserved Burn/Shock variant declarations

## Proof Level

- This slice proves: Deterministic integration tests over a minimal Bevy `App` fixture (mirroring `turn_system/tests.rs` pattern) — three new `tests/status_*.rs` files exercise refresh-max-dur, multi-kind coexistence, and Debuff-only cleanse. A fresh `tests/status_accuracy.rs` re-establishes accuracy-gate coverage post-deletion. No RNG outside the existing seeded `roll_pct` paths, no wall-clock.

## Integration Closure

`StatusBag` is the new shared substrate for S03-S05 per-status semantics; tick loop already iterates instances so S03+ can plug per-kind effect arms into the existing match block. `cleanse_debuffs` is exposed as a public API on the bag so M019's `Effect::EmitCleanse` can call it without further refactor. Bootstrap-seeding keeps S03+ free of "does this unit have a bag yet?" branches.

## Verification

- No new event variants. `OnStatusApplied` continues to fire on apply and on refresh (both are "apply" per canon). `OnStatusExpired` now fires once per expired instance instead of once per component removal. JSONL log surface unchanged in shape.

## Tasks

- [ ] **T01: StatusBag + BuffKind types and policy** `est:S`
  Edit `src/combat/status_effect.rs`. Keep `StatusEffectKind` enum unchanged (already canon post-S01). Replace the `StatusEffect` struct with: `StatusInstance { kind: StatusEffectKind, duration_remaining: u32 }` (Serialize/Deserialize, Clone, PartialEq, Debug; not a Component). Add `BuffKind { Buff, Debuff }` enum (Copy, Eq) and free fn `pub fn classify_buff_kind(kind: &StatusEffectKind) -> BuffKind` returning `Buff` for `Blessed` and `Debuff` for all 6 other variants (Heated/Chilled/Paralyzed/Slowed/Burn/Shock — total over the enum so reserved variants compile). Add `pub struct StatusBag(Vec<StatusInstance>)` with `#[derive(Component, Default, Debug, Clone)]`. Methods: `apply(&mut self, kind: StatusEffectKind, dur: u32)` upserts with `max(old, new)` (locate by `iter_mut().find(|i| i.kind == kind)`); `tick_all(&mut self) -> Vec<StatusEffectKind>` decrements every instance, returns kinds whose duration reached 0, removes them; `cleanse_debuffs(&mut self) -> Vec<StatusEffectKind>` drains every instance whose classify is `Debuff`, returns kinds removed; `has(&self, kind: &StatusEffectKind) -> bool`; `get_dur(&self, kind: &StatusEffectKind) -> Option<u32>`; `is_empty(&self) -> bool`; `iter(&self) -> impl Iterator<Item = &StatusInstance>`. Rewrite the existing inline `#[cfg(test)] mod tests` (lines ~80-138): drop the 7 single-component RON round-trip tests; replace with: refresh-max-dur math (apply 2 then 1 → dur=2; apply 2 then 5 → dur=5), multi-kind coexistence at unit level (apply Heated+Chilled+Blessed, all three present), `classify_buff_kind` totality (assert every variant returns either Buff or Debuff, Blessed→Buff specifically), `cleanse_debuffs` leaving Blessed intact (stage all 5 active kinds, cleanse, assert returned Vec = 4 Debuffs and `has(Blessed)` still true), `tick_all` returns expired kinds and removes them. Lock policy note as a doc-comment on `apply`: a re-apply that fails the accuracy roll does NOT call `apply` — the `roll_pct(threshold)` gate at `src/combat/turn_system/pipeline.rs:725-729` runs *before* `apply`, so a resisted re-apply emits `OnStatusResisted` and leaves the existing duration untouched. Update `src/combat/mod.rs:61` re-export: replace `StatusEffect` with `StatusBag` (and add `StatusInstance`, `BuffKind`, `classify_buff_kind` if any external crate needs them; otherwise keep private). Expect compile errors at call-sites until T02-T04.
  - Files: `src/combat/status_effect.rs`, `src/combat/mod.rs`
  - Verify: `cargo test --lib combat::status_effect` green (inline tests cover refresh-max-dur, multi-kind, classify totality, cleanse, tick_all). Rest of the tree will not compile yet — expected.

- [ ] **T02: Bootstrap-seed StatusBag + migrate apply pipeline** `est:S`
  Edit `src/combat/bootstrap.rs:160`: add `StatusBag::default(),` to the unit spawn bundle, adjacent to `RoundFlags::default()`. This eliminates the "create-on-first-apply" branch — every unit always has a bag. Edit `src/combat/turn_system/pipeline.rs:21` import: replace `StatusEffect` with `StatusBag`. Edit the apply site at `src/combat/turn_system/pipeline.rs:721-753`: replace `commands.entity(target_entity).insert(StatusEffect { kind, duration_remaining })` (line 731) with a query-driven path that fetches `&mut StatusBag` from the target and calls `bag.apply(kind, duration_remaining)`. Preserve the accuracy gate at lines 725-729 exactly — `roll_pct(threshold)` stays in front of `apply`, so resisted re-apply still fires `OnStatusResisted` and never calls `apply`. Continue emitting `OnStatusApplied { target, kind }` on both insert and refresh (refresh is still an apply per canon — emit AFTER `apply` returns, unconditional within the gate-passed branch). The query shape for accessing the bag depends on the surrounding system signature: if a `Query<&mut StatusBag>` can be added to the system params alongside the existing queries without borrow conflicts, do that; if it conflicts with an existing query holding `Entity` of the same target, add `commands.entity(target).queue(|mut entity| { ... })` with a closure that grabs the bag — pick the simplest shape that compiles. Do not change event payload shapes.
  - Files: `src/combat/bootstrap.rs`, `src/combat/turn_system/pipeline.rs`
  - Verify: `cargo check` clean for pipeline + bootstrap path. Manual read of the diff: `OnStatusApplied` fires on refresh; `OnStatusResisted` still gated by `roll_pct`; bootstrap inserts `StatusBag::default()` for every spawned unit.

- [ ] **T03: Migrate tick + expiration + both mod.rs query sites** `est:S`
  Edit `src/combat/turn_system/mod.rs`. Update the import at line 6: replace `StatusEffect` with `StatusBag`. **Migrate BOTH query sites**: (a) `ResolveActorsQuery` type alias at line 53-71 — change line 67 from `Option<&'static mut StatusEffect>` to `Option<&'static mut StatusBag>`; (b) `advance_turn_system` inline query tuple at lines 358-378 — change line 369 from `Option<&mut StatusEffect>` to `Option<&mut StatusBag>`. Do not skip either — they are independent queries, not a single declaration. Rewrite the tick path at lines 465-509: for each unit's bag, call `let expired = bag.tick_all();` and iterate `expired` to emit one `OnStatusExpired { unit, kind }` event per expired kind. The per-kind match arm at lines 479-485 (covering all 7 `StatusEffectKind` variants) stays as a no-op placeholder for S03-S05 — preserve totality over the enum so S03+ can fill arms without re-discovering the seam. Delete the `commands.entity(snap.entity).remove::<StatusEffect>()` call at line 500 — the bag persists empty after `tick_all` drains expired instances; removal would force re-insert on next apply, which is wasteful churn and breaks the bootstrap-seed invariant. Do not change event payload shapes.
  - Files: `src/combat/turn_system/mod.rs`
  - Verify: `cargo check` clean for mod.rs. Both query sites migrated (`rg "&'static mut StatusEffect|&mut StatusEffect" src/combat/turn_system/mod.rs` returns 0). Tick system emits one `OnStatusExpired` per expired kind (verified by T05 tests).

- [ ] **T04: Migrate follow_up + in-tree tests to StatusBag** `est:M`
  Edit `src/combat/follow_up.rs`. Update import at line 4: `StatusEffect` → `StatusBag`. Update `ResolveActorsQuery` type alias at line 104: `Option<&'static mut StatusEffect>` → `Option<&'static mut StatusBag>`. Confirmed via grep that no `.kind` read access exists at this site (pure ownership pass-through for downstream systems), so the migration is a pure rename. Edit `src/combat/turn_system/tests.rs`. Update the import at line 3 to add `StatusBag` (keep `StatusEffectKind`). Rewrite all 7 fixture spawns to use `StatusBag::default()` with `bag.apply(kind, dur)` pre-spawn: lines 136 (Heated), 155+162 (Heated re-apply over a primer), 183 (Chilled), 199+206 (Chilled re-apply), 234 (Paralyzed), 286+302 (Paralyzed re-apply), 324 (Heated for the world.get assert path). Pattern: `let mut bag = StatusBag::default(); bag.apply(StatusEffectKind::Heated, 2); commands.spawn((..., bag))`. Update all assertions: `app.world().get::<StatusEffect>(entity).is_none()` (lines 149, 193) → `app.world().get::<StatusBag>(entity).map_or(true, |b| b.is_empty())`; `app.world().get::<StatusEffect>(entity)` followed by `Some(&StatusEffect { kind: ..., duration_remaining: N })` pattern matches (lines 337-339) → `app.world().get::<StatusBag>(entity).and_then(|b| b.get_dur(&StatusEffectKind::Heated)) == Some(N)`. Do not bypass policy with direct field access — always go through `apply` / `has` / `get_dur`. After this task `cargo check` and `cargo test --lib` must be fully green (integration tests in `tests/` still may have stale references — handled in T05).
  - Files: `src/combat/follow_up.rs`, `src/combat/turn_system/tests.rs`
  - Verify: `cargo check` clean across the whole tree. `cargo test --lib` green. `rg 'StatusEffect\s*\{' src/` returns 0 hits (all spawns go through `StatusBag::apply`).

- [ ] **T05: Reconcile S01 drift + write fresh DoD tests + migrate combat_coherence** `est:M`
  **Destructive step (requires confirmation before execution):** Delete `tests/status_effect_apply.rs`, `tests/status_effect_integration.rs`, `tests/status_effect_turn_tick.rs` per M017-CONTEXT delete-and-rewrite-fresh directive. S01 migrated them in place instead of deleting; S02 reconciles. Their lifecycle coverage (apply/insert/refresh/expire) is now subsumed by the 3 new S02 DoD tests plus the in-tree `turn_system/tests.rs` fixtures (T04). **Delete** `tests/status_accuracy.rs` and write a fresh replacement covering the same accuracy-gate semantics under the `StatusBag` API: 3 tests (miss → `OnStatusResisted` + `bag.has(Paralyzed) == false`; hit → `OnStatusApplied` + `bag.has(Paralyzed) == true`; neutral → always-apply with threshold=100). Use the same seed-search pattern via `hit_seed` / `miss_seed` helpers if they exist in `src/combat/rng.rs`, or inline the seed constants. Add three new DoD tests, each a minimal Bevy `App` fixture mirroring `turn_system/tests.rs`: (a) `tests/status_refresh_max_dur.rs` — neutral matchup (threshold=100, accuracy never gates), apply Heated(dur=2), apply Heated(dur=1), assert exactly one Heated instance with `bag.get_dur(&Heated) == Some(2)`; then apply Heated(dur=5), assert `Some(5)`. (b) `tests/status_multi_kind_coexist.rs` — apply Heated + Chilled + Blessed to the same target via three skill-apply events, assert all three present via `bag.has(kind)` and that their durations match what was applied. (c) `tests/status_cleanse_policy.rs` — direct unit-level test (no Bevy events needed): construct a `StatusBag::default()`, call `bag.apply` five times for Heated/Chilled/Paralyzed/Slowed/Blessed; call `bag.cleanse_debuffs()`; assert returned `Vec` contains exactly the 4 debuff kinds (order-insensitive via sort or HashSet compare) and `bag.has(Blessed)` is still true / `bag.is_empty()` is false / the 4 debuff `has(...)` are false. Migrate `tests/combat_coherence.rs`: update import at line 3 to add `StatusBag` (keep `StatusEffectKind`); rewrite the helper at lines 410-417 from `Option<&StatusEffect>` to `Option<&StatusBag>` — return `bag.iter().next().map(|i| i.kind.clone())` to preserve the "first kind" semantic, or pick a more specific kind if callers (lines 926/946/967/981) expect one; rewrite the spawn at line 62 from `StatusEffect { kind: ..., duration_remaining: ... }` to `{ let mut b = StatusBag::default(); b.apply(...); b }`. **NOT in scope:** `tests/form_identity.rs` and `tests/follow_up_chains.rs` — verified `rg StatusEffect` returns 0 matches on both; the previous plan's file list overstated their inclusion.
  - Files: deletions: `tests/status_effect_apply.rs`, `tests/status_effect_integration.rs`, `tests/status_effect_turn_tick.rs`; rewrites: `tests/status_accuracy.rs`, `tests/combat_coherence.rs`; new: `tests/status_refresh_max_dur.rs`, `tests/status_multi_kind_coexist.rs`, `tests/status_cleanse_policy.rs`
  - Verify: `cargo test --test status_refresh_max_dur`, `cargo test --test status_multi_kind_coexist`, `cargo test --test status_cleanse_policy`, `cargo test --test status_accuracy`, `cargo test --test combat_coherence` all green individually. Full `cargo test` green with 0 failed, 0 ignored. The 3 deleted files no longer exist (`ls tests/status_effect_*.rs` returns empty).

- [ ] **T06: Smoke + grep guard + SUMMARY** `est:S`
  Run the headless smoke CLI: `cargo run --bin combat_cli` and confirm exit 0 with no panics. Re-run the S01 grep guard `rg -En '\b(Burn|Freeze|Shock|DeepFreeze)\b' src/ tests/` and confirm only reserved Burn/Shock variant declarations remain (no new legacy references introduced by S02; no Freeze/DeepFreeze anywhere). Run `rg "StatusEffect\b" src/ tests/` and confirm zero matches — every reference now uses `StatusBag` or `StatusEffectKind` (the enum, which keeps its canonical name). Run `rg "&'static mut StatusEffect|&mut StatusEffect|&StatusEffect" src/` — must return 0. Confirm `cargo check` and full `cargo test` both green (0 failed, 0 ignored). Produce `.gsd/milestones/M017/slices/S02/S02-SUMMARY.md` via `gsd_complete_slice` describing: the `StatusBag` API surface for S03-S05 (apply/tick_all/cleanse_debuffs/has/get_dur/iter), the `BuffKind` classification and cleanse hook for M019's `Effect::EmitCleanse`, the bootstrap-seed pattern, the S01 drift reconciliation, and the two mod.rs query sites discovered during migration. Note the deferred work: per-status semantics (S03-S05), source attribution on instances (M020), `BuffKind::DR/Aura/Mark` (M019).
  - Files: `.gsd/milestones/M017/slices/S02/S02-SUMMARY.md`
  - Verify: Smoke CLI exits 0. Grep guards all clean. `cargo test` 0 failed / 0 ignored. SUMMARY.md persisted via `gsd_complete_slice`.

## Files Likely Touched

- src/combat/status_effect.rs
- src/combat/mod.rs
- src/combat/bootstrap.rs
- src/combat/turn_system/pipeline.rs
- src/combat/turn_system/mod.rs
- src/combat/turn_system/tests.rs
- src/combat/follow_up.rs
- tests/status_refresh_max_dur.rs (new)
- tests/status_multi_kind_coexist.rs (new)
- tests/status_cleanse_policy.rs (new)
- tests/status_accuracy.rs (delete + rewrite fresh)
- tests/status_effect_apply.rs (delete)
- tests/status_effect_integration.rs (delete)
- tests/status_effect_turn_tick.rs (delete)
- tests/combat_coherence.rs (migrate helper + spawn)
- .gsd/milestones/M017/slices/S02/S02-SUMMARY.md
