# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Per-Digimon blueprint implementation for Twin Core mechanics. | Agumon and Gabumon blueprints process character-specific custom signals into Twin Core tag transitions. | Decouples character-specific mechanic logic from the core combat kernel while maintaining data-driven control via skills.ron. **Parzialmente superseded da M021/D010 (2026-05-13):** il path legacy via `CombatKernelTransition::TwinCore` (variant kernel-aware Digimon-specific) viene rimosso in M021 fascia B; Twin Core resterà implementato come blueprint, ma le transitions saranno espresse via `ctx.enqueue(Intent::...)` sul nuovo `SkillCtx`, non come variant kernel-typed. Il principio di decoupling resta valido; cambia solo il meccanismo. | Yes | agent |
| D002 | Pre-M017 planning, SP1 kernel primitives spike | combat-kernel | Reactive signature bus uniforme per OnKill / OnHitN / OnStatusApplied / OnUltimateUsed | Uniformare gli eventi reactive su CombatEventKind esistente (estendere UnitDied payload con status_remaining + heated_remaining; aggiungere StatusApplied, UltimateUsed). OnHitN resta FSM-side, non bus-side (canon §C4 row 4). | SP1 audit: 4 reactive verbs servono ai blueprint (Twin Core, Predator Loop, Echo, Kitsune Grace). Riusare il bus esistente evita un secondo canale e mantiene il contract single-source-of-truth (CLAUDE.md convenzione eventi). FSM topology copre OnHitN gratis. | yes — riapribile se M018 enemy roster richiede un canale separato per AI hooks | agent |
| D003 | Pre-M017 planning, SP1 kernel primitives spike | combat-kernel | Time-manipulation primitive: split AdvanceTurn / DelayTurn con cap ±50% e clamp [0,200] | Sostituire l'attuale TurnAdvance signed (amount_pct + AV accumulator) con due varianti distinte AdvanceTurn(pct) e DelayTurn(pct), entrambe capate ±50% per chiamata, con gauge clamp [0,200] applicato dopo somma. Niente accumulator AV pre-cap. | Canon §02-02b C2.1 richiede semantica esplicita (advance vs delay) per Renamon Tōhakken + Kitsune Grace + telegraph UI. Cap + clamp formali sono già nel design ma non nel codice. Split rende l'intent leggibile a colpo d'occhio nei test e nei log JSONL. | no — è canon design lock-in di round-3 | agent |
| D004 | Pre-M017 planning, SP1+SP3 convergente | combat-status | Status taxonomy v0 rewrite: Heated / Chilled / Paralyzed / Slowed / Blessed | Rimuovere Burn / Freeze / Shock / DeepFreeze. Introdurre i 5 status canon §H.1 con single-instance + refresh_max_dur. Heated multi-stack (cap definito da skill, non globale fisso). Confused dropped. | Ogni skill identity del roster M017 si poggia sui 5 status canon. Mantenere la vecchia tassonomia richiederebbe traduzione per ogni Effect e renderebbe i log incomprensibili. Refresh_max_dur (vs additive) è la regola che il design ha già lock-in nel round-3. | no — canon design | agent |
| D005 | Pre-M017 planning, SP1 kernel primitives spike | combat-damage | DR pipeline modulare con taxonomy intra-unit max-replace + cross-unit additive clamp 0.5 | Aggiungere BuffKind::DR e mitigation layer in damage.rs. Più DR sulla stessa unit (es. fur_cloak + holy_aegis su Gabumon) → max-replace (vince il maggiore). DR cross-unit (sommare percentuali su un singolo target da fonti distinte) → additive con clamp finale a 0.5 (50% max). | Canon §02-08 H.3 formalizza la regola; oggi il damage pipeline non ha layer DR e impedisce Gabumon fur_cloak/blue_cyclone + Patamon holy_aegis. Max-replace intra-unit evita stacking degenere; additive cross-unit con clamp evita immortality builds. | parziale — il clamp 0.5 è un numero di balance, riapribile in M018+ playtest | agent |
| D006 | Pre-M017 planning, SP1+SP3+SP4 convergente | combat-targeting | TargetShape resolver: implementare Blast / AoE(All) / Bounce(N) + selectors estesi | Espandere il resolver oltre Single, gated oggi da UnimplementedTargetShape. Aggiungere Blast (target + adiacenti), AoE{side} (All/Enemies/Allies), Bounce{hits, selector} con tie-break slot_index asc. Selectors aggiuntivi: AdjLowest, LowestHpPctAlive, RandomEnemyAlive{seed}, SingleAlly. Loosen il validator skills.ron di conseguenza. | 15/24 skill richiedono shape non-Single; senza questo lo scope M017 si dimezza (no Agumon ult, no Renamon, no Patamon ult, no Tentomon skill+ult). Tie-break deterministico (slot_index) è già canon §8 per Echo/Chain. | no — canon design | agent |
| D007 | Pre-M017 planning, SP2 blueprint API spike | architecture | Blueprint API: trait Blueprint + BlueprintRegistry vuoto + Bevy plugin self-registration | Introdurre trait Blueprint (key + commit_signals + on_event + snapshot, ultimi tre con default no-op) e BlueprintRegistry come Resource Bevy popolata via plugin-per-Digimon (AgumonPlugin, GabumonPlugin, ...). Dispatcher kernel generico: reg.get(&sig.owner).commit_signals(...). Unica centralizzazione: add_plugins((...)) in main.rs (1 riga per blueprint). **Firma trait evoluta da D010 (2026-05-13):** target post-M021 è `owner`/`dispatch`/`build` con dispatch che riceve `&mut SkillCtx` e usa `ctx.enqueue(Intent::...)` invece di `commit_signals`/`on_event`/`snapshot` direttamente sullo stato. Il pattern plugin+registry resta; cambia la signature interna del trait per allinearsi al kernel come unico esecutore di Intent. Vedi `M021-RESEARCH.md §2.3` e `M021-CONTEXT.md`. | Oggi aggiungere un blueprint = 8 site da toccare e CombatKernelTransition con 5 varianti Digimon-tipizzate. Pattern plugin chiude la regressione: nuovo blueprint = nuovo file + 1 riga. Allineato a hybrid SP3 (Effects in RON, listener filter in Rust). Estende a M018+ enemy roster senza modifiche al kernel. | parziale — la firma trait è esplicitamente evoluta (vedi nota Decision); state-per-unit (D-M017-BLUEPRINT-STATE-PER-UNIT) deferred a M018+ | agent |
| D008 | Pre-planning M017. Decisione presa post-spike SP1-SP5 e dopo accordo sul portfolio 14-milestone (M017-M029) con visual tracer Agumon end-to-end al M023. | architecture | Separazione runtime headless / windowed via composizione di Bevy Plugin, gameplay logic non-duplicato | Estrarre CombatPlugin (kernel + blueprint registry + asset loader, tutto headless-safe) come unica fonte di gameplay logic. Headless binary = MinimalPlugins + CombatPlugin + CliPlugin. Windowed binary = DefaultPlugins + CombatPlugin + RenderPlugin + UiPlugin. Feature gate `windowed` confinato a 2 file plugin (render/plugin.rs, ui/plugin.rs). CombatPlugin non importa winit/wgpu/egui. Eventi CombatEvent restano single-source-of-truth letti dai render/UI systems via EventReader. | Il pattern target era già parzialmente implicito (src/headless.rs + src/windowed.rs split, #[cfg(feature = "windowed")] confinato a src/ui/) ma non formalizzato come Plugin. Senza formalizzazione, il rischio è che M023 (visual tracer Agumon) introduca render systems non-gated che rompono headless silently. Plugin composition rende esecutiva la convenzione CLAUDE.md "headless first": test integration usano CombatPlugin + MinimalPlugins, garantendo che ogni gameplay regression sia catturata senza UI; ogni glitch visivo è isolato a render/UI plugin. Zero duplicazione di event handling, status taxonomy, FSM loader. Implementazione naturalmente collocata in M021 come prima slice (refactor di register_combat_kernel_runtime in CombatPlugin), pre-requisito per il trait+registry+migrate delle slice successive. | soft — la separazione plugin è strutturale; eventuali ulteriori split (es. CombatPlugin → CombatKernelPlugin + BlueprintRegistryPlugin) sono evoluzioni interne che non rompono il contract headless/windowed. | collaborative |
| D009 | M017 pre-S01 kickoff, post planning del milestone roadmap. Drift identificato leggendo il piano M017-ROADMAP.md (che già seguiva canon §H.4/§H.5) vs D004 (multi-stack) | combat-status | Corrigendum a D004: Heated è single-instance in v0, NON multi-stack. Stack-aware mechanic differita a M018+ (allineamento a canon §H.5) | Sovrascrive la clausola "Heated multi-stack (cap definito da skill, non globale fisso)" di D004. In v0 (M017) tutti i 5 status (Heated/Chilled/Paralyzed/Slowed/Blessed) sono single-instance per target con policy refresh_max_dur. La terminologia "stack" usata nel roster minimo (08_roster_minimal.md: "+1 Heated stack", "+2 Heated") è linguaggio di design intent (Heated viene riapplicato e refresha la durata) ma NON implica un counter numerico per-stack in v0. Skill che vogliono "Heated × N" con DoT scaling sul count (es. §H.5 gap 2) sono esplicitamente deferred a M018+. Le altre clausole di D004 (rimozione Burn/Freeze/Shock/DeepFreeze, refresh_max_dur, Confused dropped) restano valide. | Drift origin: D004 è stata sintetizzata durante SP1+SP3 leggendo il canon §08_roster_minimal.md ("Heated stacks", "+2 Heated") e interpretando "stack" come mechanic multi-instance con counter. Ma canon §02-08 §H.4 lock-in round-3 dice esplicitamente "ogni status è single-instance per target (no stacking). Re-applicazione segue policy refresh_max_dur" e §H.5 differisce esplicitamente "Stack-aware status numerici (Heated × 3 DoT scaling)" a post-M017. Il roster minimo usa "stack" come gergo di design intent (apply ripetuto = refresh durata), non come mechanic. SP1+SP3 audit non hanno riconciliato i due passaggi del canon, generando drift su D004 prima che il codice fosse toccato. Chiudere ora evita confusione downstream durante S01-S06 di M017 e mantiene il contratto "DECISIONS.md è current state, non append narrativo". | no — chiude un drift documentale, sovrascrive D004; nuova decisione su stack-aware mechanic arriverà in M018+ se/quando il design lo richiede | collaborative |
| D010 | 2026-05-13, durante M018 S03 (Bounce + selector). Discussione architetturale post-spike SP-skill-dsl-coverage. Vincolo utente: logica skill in Rust, no hot-reload, RON solo numeri. Decisione presa prima dell'apertura di M021. | architecture | Shape della Skill API post-foundation: enum `Effect` chiuso (v0) vs `trait Skill` aperto vs scripting embedded (Rhai/Rune/Lua) | Adottare `trait Skill::resolve(&mut SkillCtx, &Params)` in Rust puro, con split netto **query** (read-only: `predict_damage`, `adjacents`, `can_target`, `sp_available`, …) / **enqueue** (write-deferred: `ctx.enqueue(Intent::DealDamage { … })`, `Intent::ApplyStatus`, `Intent::FollowUp`, `Intent::AdvanceTurn`, `Intent::DelayTurn`, …). Il kernel resta unico esecutore degli `Intent` (formula damage, mitigation, break, status tick, event bus). RON tiene solo numeri/tag (dmg, hops, sp_cost, scaling, target_shape base), non logica. Niente scripting embedded. Migration delle skill esistenti (Bounce, Blast, AoE già in M018) come parte di M021. | (a) enum chiuso è cerimonia inutile in assenza di hot-reload e con logica in Rust: niente data-DSL, niente parser; (b) scripting embedded (Rhai/Rune) introduce non-determinismo bit-level tra versioni, sandbox fragile, debug stack misto Rust/script, 10-100× slowdown, rompe i tests headless deterministici (`next_bounce_hop` ecc.); `bevy_mod_scripting` Rune è "on hold"; (c) trait + ctx dà flessibilità di scripting con type-safety, determinismo bit-identico, debug nello stesso stack Rust; pattern allineato a MTG GRE (skill produce game actions, motore le risolve). Lo split query/enqueue è critico: skill = scheduler di intent, kernel = unico esecutore → P001 rafforzato (un solo damage path, niente duplicazione formula). Predizione necessaria a UI (highlight/tooltip), AI (decision routing), e alla skill stessa per scelte mid-loop (es. bounce next-hop su `&out` già accodato). Bounce M018 con `next_bounce_hop` puro + selector tipizzato è già proof-of-concept del pattern. Single-dev: ricompilare per nuova skill costa meno che mantenere parser+sandbox+debugger di un lang embedded. | soft — la lista delle primitive `SkillCtx` (query) e degli `Intent` (write) evolverà incrementalmente durante la migrazione M021. Escape hatch per modding via scripting embedded resta una post-1.0 option, non architetturale ora. | collaborative |
