# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

<!-- 2026-05-15: D001-D033 ricostruite a mano dopo reset gsd.db.
     D001-D015 + D023-D024 = contenuto integrale dal commit 36c4bf5.
     D025-D033 = sintesi compatta dai findings spike pass-3 (originali persi nel reset, scritti più stringati). -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | Slice M019/S04 planning — closes follow-up #3 from M018 (PerHop length guard) | kernel-combat | DamageCurve::PerHop runtime behaviour when curve length is less than hops_planned | Truncate the bounce loop to v.len() and emit a CombatEventKind::OnActionFailed diagnostic event before the loop. Kernel never panics. Load-time validator in skills_ron.rs remains the primary defence; the runtime guard exists for dynamically constructed ResolvedActions (future blueprint emitters, tests) that bypass it. | Silent clamp masks bugs in blueprint emitters M020+ will introduce. Truncation preserves total resolution (no half-applied action, no panic, action bounces what it can) and matches the existing pool-exhaustion truncate pattern at pipeline.rs:844. Reusing OnActionFailed keeps event taxonomy stable and aligns with how other kernel anomalies are reported (SP shortfall, attacker stunned/KO). Diagnostic event makes the bug observable in JSONL traces — combat events are the single source of truth. | Yes; if a dedicated event variant proves more useful for tooling, the diagnostic can switch to OnDamageCurveTruncated without changing the truncate-with-event semantics. | agent |
| D002 | M021 fascia A — apertura milestone, post-research §7.3 | M021 — kernel-combat / skill API | Forma di `Intent` (transition prodotta da `trait Skill::resolve`) | Enum canon chiusa di ~17 varianti core (Damage, Heal, ApplyStatus, RemoveStatus, ChangeSP, ChangeUlt, AdjustToughness, Stun, MoveInTurnOrder, EnqueueFollowUp, …) + variant catch-all `BlueprintSignal { owner: &'static str, payload: Box<dyn Any + Send + Sync> }` come escape hatch typed-owner + variant `Reject { reason: LegalityReason }` per il flusso UI (dry-run). | Tiene il kernel agnostico (P001): le formule core (damage, mitigation, break, status tick) restano single-source-of-truth dentro il kernel; le skill non possono mutare stato direttamente, solo dichiarare intent. L'escape hatch `BlueprintSignal` consente meccaniche franchise-specifiche (Twin Core, Predator Loop) senza inquinare l'enum core. `Reject` rende UI-friendly il riuso della stessa `resolve` in dry-run senza branch separati. Mapping 1:1 con l'attuale `enum Effect` garantisce migrazione meccanica del 90% delle skill canon (24/24 oggi coperte da 13 varianti `Effect`). | Sì; aggiungere nuova variant core all'enum è breaking-compat ma controllato (un solo punto di estensione, kernel). `BlueprintSignal` invece evolve senza toccare il kernel. | collaborative |
| D003 | M021 fascia A — apertura milestone, post-research §7.4 | M021 — kernel-combat / skill API | Dove vive la validation pre-skill (legality / can-cast) | Single entrypoint: il kernel esegue le pre-check oggettive (target valido, SP/Ult sufficienti, stunned, range/team, status che bloccano), poi `Skill::resolve(&mut SkillCtx)` può rifiutare via `ctx.reject(reason)` (sticky: una volta rifiutato, il drain di quella resolve non emette nulla salvo il `Reject`). UI chiama esattamente la stessa `resolve` in `Mode::DryRun` per ottenere `SkillPreview` (che include il `Reject` se presente). | Evita la duplicazione attuale tra `action_query.rs::query_intent_legality` e la logica di esecuzione: oggi le regole sono espresse in due posti che devono restare sincronizzati. Con un solo entrypoint, la skill è l'autorità sulla propria can-cast. `query_intent_legality` post-M021 diventa thin wrapper su un dry-run della `resolve`. Sticky reject + drain destinations separati per Mode garantiscono che dry-run e execute usino lo stesso codice path → nessuna divergenza preview/realtà. | Sì; se emergesse pressione per pre-check personalizzabili da blueprint (es. una mechanic vieta certe skill globalmente), si può aggiungere un hook `Blueprint::pre_check` senza cambiare la divisione di responsabilità qui sopra. | collaborative |
| D004 | M021 fascia A — apertura milestone, post-research §7.6 | M021 — UI / skill preview contract | Come la UI ottiene damage preview / status preview / legality reasons prima dello use | Funzione `query_skill_preview(world, caster, skill_id, target_selection) -> SkillPreview` in `action_query.rs` che esegue `Skill::resolve` in `Mode::DryRun` e aggrega gli Intent in uno struct `SkillPreview { damage_per_target, status_applied, healing, sp_delta, ult_delta, follow_ups_telegraphed, blueprint_signals, reject }`. `combat_panel.rs` consuma `SkillPreview` e smette di chiamare `predict_damage` locale duplicato. | Disaccoppia la UI dalle formule (P001): la UI legge metadata già aggregati invece di replicare logica kernel. Garantisce coerenza preview ↔ esecuzione perché entrambi passano attraverso la stessa `resolve`. Blueprint signals esposti nel preview consentono a UI di mostrare effetti franchise-specifici (es. "Twin Core: Heat +1") senza che la UI conosca semantica `twin_core`. Migrazione clean: tutto `combat_panel.rs` legge una sola API. | Sì; lo schema di `SkillPreview` è puramente additivo (la UI ignora campi che non capisce). Future skill con telegraph richieste UI-specifiche aggiungono campi senza breaking. | collaborative |
| D005 | M021 fascia B — apertura milestone, post-research §8 Q-B1 | M021 — kernel-combat / blueprint extensibility | Come strutturare le meccaniche franchise condivise tra più Digimon (Twin Core, Predator Loop, Holy Support, …) | Pattern "shared-mechanic mini-plugin": ogni meccanica condivisa è un blueprint a sé stante (es. `blueprints/twin_core/` con `owner: &'static str = "twin_core"`), che implementa `trait Blueprint` e possiede stato, hook, e processing degli `BlueprintSignal` con il proprio owner. I Digimon che la usano (Agumon, Dorumon, …) emettono `Intent::BlueprintSignal { owner: "twin_core", payload }`; il dispatch del kernel route al plugin owner. Niente owner+guest, niente coupling tra Digimon e mechanic-host. | Risponde al criterio falsificabile M021 §7.9 (aggiungere un nuovo Digimon Twin-Core-user = file solo in `blueprints/<x>/`, zero edit in `twin_core/`). Disaccoppia identità Digimon (cosmetica + signal emission) dalla mechanic (state machine + processing). Estendibile: aggiungere una nuova shared mechanic = aggiungere `blueprints/<mech>/`, non toccare quelle esistenti. Pattern bevy-idiomatico: ogni mechanic è un plugin discreto con i propri sistemi e resource locali. | Sì; se due shared mechanic si scoprissero strettamente accoppiate (es. Twin Core risuona con Holy Support), il pattern degenererebbe a un singolo mega-plugin che le include entrambe — soluzione meccanica, non architetturale. | collaborative |
| D006 | M021 fascia B — apertura milestone, post-research §8 Q-B2 | M021 — kernel-combat / blueprint extensibility | Tipo del payload di `Intent::BlueprintSignal` — opaque vs enum chiusa | `Box<dyn Any + Send + Sync>` opaque al kernel, con invariante `owner ↔ tipo concreto` blindata dentro il plugin owner via helper `MechApply::into_transition()` / `MechApply::from_transition(&t) -> &Self` (associated `const OWNER: &'static str` + downcast debug-checked). | L'alternativa enum chiusa blueprint-side (`enum BlueprintTransition { TwinCore(_), BatteryLoop(_), … }`) ricrea il coupling che M021 vuole eliminare, solo spostato di un livello: aggiungere Tailmon richiederebbe editare `blueprints/mod.rs`, violando il criterio §7.9. Con `Box<dyn Any>` il kernel ha conoscenza zero e ogni shared mechanic possiede la propria enum locale; il downcast resta confinato a 2 righe nel plugin owner e l'invariante owner↔type è enforced dal helper. Cost: un downcast per tick (trascurabile); benefit: kernel chiuso a estensione runtime. | Sì; se il numero di shared mechanic restasse stabilmente piccolo (≤3) e i bug di mismatch owner→type fossero ricorrenti, si potrebbe valutare typed-id (newtype TypeId-based) — refactor meccanico, non riscrittura. | collaborative |
| D007 | M021 fascia B — apertura milestone, post-research §8 Q-B3 | M021 — kernel-combat / blueprint extensibility | Lifecycle del `BlueprintRegistry` (HashMap owner → Box&lt;dyn Blueprint&gt;) | Startup-frozen: `BlueprintRegistry` resta `Resource`, registrazione avviene una sola volta in `BlueprintsPlugin::build()`, API pubblica espone solo `get(owner)` / `iter()`. Il metodo `register` è `pub(crate)` e non viene mai chiamato dopo lo startup. Hot-reload / DLC dinamico deferred. **SUPERSEDED de facto da D032** (spike pass-2/3): `trait Blueprint + Box<dyn>` rimosso dal critical path; ogni Digimon è un modulo con `pub fn register(reg: &mut ExtRegistries)` che installa fn-by-id sulle 7 registry parametriche. Vedi D035 per audit-trail formale. | M021 non richiede hot-reload, M022+ neanche. Modding/DLC sono ipotesi a 12+ mesi. La complessità di "single-writer mutabile + invalidazione cache derivate" (RosterEntry, ValidationSnapshot, SkillRegistry) è costo certo per beneficio ipotetico. Startup-frozen mantiene la stessa garanzia del `const BLUEPRINTS: &[…]` attuale ma dietro un trait. | Superseded da D032/D035. | collaborative |
| D008 | M021 pre-roadmap — Q7 settled | M021 — kernel-combat / observability | `Intent::BlueprintSignal` routing nel transition stream (single-layer vs double-layer) | **Double-layer**: l'`Intent::BlueprintSignal { owner, payload }` non viene risolto direttamente dal plugin owner. Passa prima per una variant dedicata `CombatKernelTransition::Blueprint { owner, payload }` emessa dal `intent_applier`, raccolta dal `OnKernelTransition` observer e serializzata nel JSONL come ogni altra transition. **Poi** il dispatcher route al plugin owner per l'applicazione effettiva al `BlueprintState`. Costo: un'allocazione di transition + un dispatch in più per ogni signal. Post-spike: la shape definitiva si unifica in un singolo `intent_applier` che emette transition + applica; aggiornare durante S04. | Single-layer (signal applicato direttamente dal plugin senza passare dal transition stream) renderebbe invisibili al journal le mutazioni di state Digimon-specifiche: bug "Agumon doveva avere 3 stack di Twin Core ma ne aveva 0" diventa non-debuggabile da JSONL — niente replay, niente forensics. Double-layer preserva l'invariante "ogni mutazione di state visibile dall'esterno passa dal transition stream"; il costo per signal è O(1), trascurabile rispetto alla pipeline damage/status. Coerente con il pattern già in uso per kernel transitions (DamageApplied, StatusApplied, ToughnessChanged). | Sì; se in futuro il volume di signal saturasse il JSONL (improbabile a regime di 6 Digimon × ~3 signal/turno), il single-layer resta accessibile come optimization point. Reversibile via opt-in flag sul plugin owner. | collaborative |
| D009 | M021 pre-roadmap — Q8 + Q15 settled | M021 — kernel-combat / skill API | Modello di esecuzione delle skill — freeze totale vs hook-cascade cast-scoped + listener globale bus-side | **Hook-cascade cast-scoped + listener bus-globale, due trait separati.** (1) `trait Ability::hooks(&self) -> Vec<AbilityHook>`: hook **intra-skill** filtrate automaticamente dal kernel sul `cast_id` corrente. Vedono solo gli eventi della propria cast chain. Esempio: Renamon Koyosetsu `on_hit { damage_dealt } → enqueue Heal(damage_dealt/2)`. (2) `trait BlueprintListener::listeners(&self) -> Vec<EventListener>`: listener **passive cross-Digimon** subscribed al bus globale, vedono tutti gli eventi. Esempio: Patamon Holy Aegis `on_ally_hp_below(50%) → enqueue Heal`. Il kernel garantisce: `AbilityHook` matcha solo `event.cast_id == current`; `BlueprintListener` ignora `cast_id`. Ogni hook è una funzione pura `(Event, Ctx) → Vec<Intent>`. La promessa formale `preview ≡ execute` è preservata simulando la stessa cascata di hook contro un `FrozenSnapshot` invece che contro il World live. | Freeze totale (snapshot pre-cast immutabile per tutta la durata della skill) avrebbe reso awkward i chain "danno → cura per X% del danno fatto" — l'autore avrebbe dovuto passare metadata fra Intent invece di leggerli dal World. Il modello hook-cascade rende naturale "leggi l'ultimo stato di questa cast chain". Due trait separati per `AbilityHook` vs `BlueprintListener` impediscono a compile-time il bug "Agumon casta, Patamon reagisce all'hit della cast altrui come se fosse suo hop"; il filtro `cast_id` è imposto dal kernel, non dall'autore. Funzioni pure mantengono determinismo e testabilità senza ECS. | No (load-bearing per la composizione skill ↔ passive); cambiare il modello dopo M021 invaliderebbe ogni skill scritta. Eventuali ottimizzazioni (dispatch batching) restano possibili senza cambiare semantica. | collaborative |
| D010 | M021 pre-roadmap — Q11 + Ult ridisegno | M021 — kernel-combat / skill API + turn economy | Forma del `trait Ability` (capability slot vs in-trait), Ultimate cast model, multi-turno/telegraph, cost on-miss | (1) **Default-impl per tutti i metodi opzionali** del trait `Ability`: skill banali = 10 righe (`fn hooks(&self) -> Vec<Hook> { vec![] }` ecc.), niente boilerplate. (2) **Ultimate cast istantaneo**: l'Ult non consuma il turno e può essere lanciata in qualsiasi momento dentro il proprio slot di azione. Il design attuale "Ult consuma turno" viene esplicitamente ridisegnato — il blueprint Ult attuale di Renamon (Tohakken) e analoghi vanno aggiornati. (3) **Telegraph multi-turno deferito**: nessun caso d'uso concreto ora; se in futuro servirà, si introduce `Box<dyn TelegraphSchedule>` come capability slot opzionale senza breaking change. (4) **Niente `cost_adjustment` su miss/refund**: design intorno al miss è anti-pattern; il trait non avrà questo metodo. | Default-impl Rust è la risposta corretta alla preoccupazione "tanti metodi = tanto boilerplate": l'autore scrive solo ciò che serve, gli altri restano al default. Ult instant chiarisce la semantica turno (un turno = una skill normale; Ult è un'azione extra a budget separato), elimina lo sgambetto progettuale "Ult deve sentirsi potente ma consuma il turno principale" → o consuma o no, e scegliamo no. Telegraph deferred = capability slot post-M021 invece di in-trait perché 1-2 skill su 24 non giustificano espandere la API surface ora. Cost-on-miss rifiutato per evitare di incentivare design centrati sul rumore stocastico (miss). | Sì, parzialmente. Telegraph è additivo (capability slot post-M021 non breaking). Ult instant è reversibile ma costoso (richiede ridisegno bilanciamento). Default-impl strategy è invariante. | collaborative |
| D011 | M021 pre-roadmap — Q9 settled | M021 — kernel-combat / turn pipeline | Comportamento del kernel quando un target diventa invalido (KO) prima o durante un cast | **Turn-phase order esplicito + MultiHitOnKO dichiarativo.** Sequenza turn-start canonica: `TurnStart → (1) status/DoT tick + apply damage → (2) resolve KO da tick → (3) apply turn-start passive (`BlueprintListener::OnTurnStart`) → (4) resolve KO da passive → (5) build TargetableSnapshot fresca (dead units escluse) → (6) player/AI selects skill + target dal candidate pool → (7) cast → TurnEnd`. Questo elimina il caso "ho scelto Tentomon ma è morto da passive": al passo 6 Tentomon non è nel pool. Per il caso intra-cast multi-hit (skill colpisce stesso target N volte, target muore al primo hit), `TargetShape` guadagna una variante `MultiHit { hits: u8, on_ko: MultiHitOnKO }` con `MultiHitOnKO ∈ { Skip, Redirect(BounceSelector), Stop }`. Default per nuove skill multi-hit: `Redirect(LowestHpAlive)`. AoE con target morto in lista: filtrato dalla snapshot fresca, no skip-event spammato. Bounce: già coperto dal selector esistente. | Risolvere "target morto" a runtime tramite auto-retargeting kernel-side renderebbe la skill non-prevedibile per UI/AI. Spostare il problema alla fase turno (snapshot pre-cast con KO già risolti) lo elimina per costruzione. Per il multi-hit intra-cast — dove KO può accadere fra hop — la policy è esplicita nello `TargetShape`, allineata al pattern già usato per `Bounce` (D002 + M018). Riusa il rebuild-snapshot-per-hop pattern già consolidato. | Sì; aggiungere nuovi `MultiHitOnKO` variant è additivo. Il turn-phase order è invariante semantico — cambiarlo dopo che le passive lo assumono significherebbe rivedere ogni passive. | collaborative |
| D012 | M021 pre-roadmap — Q10 settled | M021 — kernel-combat / blueprint registration | Garanzia che un `Blueprint` non possa essere registrato senza il suo `BlueprintState` associato | **Type-level enforcement via `trait Blueprint { type State: BlueprintState + Component + Reflect; const ID: &'static str; ... }`** e funzione di registrazione unica `fn register_blueprint<B: Blueprint>(app: &mut App)`. **SUPERSEDED da D035**: post-spike non c'è più `trait Blueprint`; il pattern register() per-Digimon installa direttamente lo state via `app.register_blueprint_state::<State>()` se serve (D034 ne è il write-path). | Type-level enforcement era la risposta corretta a "dimenticare di registrare lo state" sotto il vecchio modello trait. Sotto fn-by-id il rischio scompare: una sola fn `register()` per Digimon contiene tutte le installazioni. | Superseded da D035. | collaborative |
| D013 | M021 pre-roadmap — Q13 settled | M021 — kernel-combat / event dispatch | Ordine di esecuzione dei `BlueprintListener` quando più blueprint sono subscribed allo stesso evento | **Tiebreaker chain deterministico `(speed_initiative DESC, slot_index ASC, team_id ASC)`.** Esempio: Patamon e Tentomon entrambi con `OnAllyKO`. Pesca prima il listener con initiative più alta; pari iniziativa, slot più basso; pari slot, team player prima. Implementato nel kernel listener dispatcher, mai delegato all'autore. | Senza ordinamento deterministico, due passive con stesso trigger producono race condition che si manifestano come bug di replay (stesso seed, esito diverso). Initiative-first è coerente con il turn order già esistente; slot-first è il tiebreaker naturale del battlefield layout; team_id chiude l'edge case a costo zero. | Sì; aggiungere chiavi tiebreaker è additivo. | collaborative |
| D014 | M021 pre-roadmap — Q14 settled | M021 — kernel-combat / safety | Cascade depth limit per hook ricorsive | **Nessun cap a regime; circuit breaker debug-only @256 eventi per singolo cast.** In `cfg(debug_assertions)`, se un singolo cast emette > 256 eventi totali, il kernel panicka con stack trace del blueprint colpevole. In release: nessun cap, l'autore della skill è responsabile dei propri budget (declarati nello `BlueprintState`). | Cap fisso lato kernel sarebbe arbitrario e maschera il bug autoriale invece di esporlo. La proposta rispetta P001 (kernel neutro). Il circuit breaker debug-only è una rete di sicurezza per loop infiniti durante lo sviluppo senza imporre un cap arbitrario in produzione; costo zero a regime. | Sì; aggiungere un cap kernel in release è additivo. La soglia 256 è regolabile. | collaborative |
| D015 | M021 pre-roadmap — Q1–Q6 blocco difese | M021 — kernel-combat / skill API surface | Conferma di sei micro-decisioni di design | (1) **Q1 Skill ID**: `String` (es. `"agumon.baby_burner"`). Typed wrapper post-MVP. (2) **Q2 Blueprint state**: `trait BlueprintState: Component + Reflect` (poi superseded da D034/D035: HashMap key-by-string). (3) **Q3 SkillCtx accessor**: metodi mirati, nessun blob `&UnitSnapshot`. (4) **Q4 Auto-consume status**: meta-flag `StatusDef { auto_consume_on_read: bool }`. (5) **Q5 enum Effect drop**: rimosso a fine S-A5. (6) **Q6 Blueprint snapshot**: `trait BlueprintSnapshot: Debug + Serialize`. | Sei decisioni con tradeoff modesti raggruppate per audit trail. Ogni scelta privilegia leggibilità RON, uniformità Bevy, stabilità API, riduzione bug-surface autoriale, un solo modo di scrivere skill, singolo punto di serializzazione. | Sì; ogni sub-decision è revocabile in isolation. | collaborative |
| D023 | M021 pre-S01 — chiude tensione `OnHit` enum vs hook fn rilevata in M021-ROSTER-FIT-DRAFT | M021 — kernel-combat / skill API | Forma dei chain hook (`on_cast` / `on_hit` / `on_kill` / `on_miss` / `on_final_hop`) — enum dichiarativo vs fn Rust | **Hook fn Rust** (fn pointer registrato in `Registry<HookExt>`, no `Box<dyn Fn>`). Registrate sull'`Ability` come capability slot opzionali. **Niente** enum `OnHit`/`OnKill`/`OnMiss`/`OnFinalHop`. Gli hook producono `Intent` via `ctx.enqueue(...)`. Il "record serializzabile" per replay/journal/AI è lo stream di `Intent` e `CombatEvent` emessi dagli hook, non il corpo della closure. Pattern unificato: passive listener, per-deal chain hook, e on-final-hop sono tutti la stessa forma `(event, ctx) → enqueue Intent`. | Enum dichiarativo era inconsistente con D009: i `BlueprintListener` passive sono già fn Rust pure, ma i chain hook on-hit/on-kill erano cristallizzati a match-enum. Promuovere tutto a fn elimina N varianti enum, mantiene il kernel invariante quando arrivano nuovi side-effect, e rende naturali condizionali e composizione di N effetti come sequenze di `ctx.enqueue`. Serializzazione preservata dal lato Intent/CombatEvent. Preview UI/AI tooltip resta possibile via dry-run dello stesso hook (vedi D024). Coerente con P001 e con la promessa formale `preview ≡ execute` di D009. | No (load-bearing). Aggiunte additive (es. `Ability::tooltip` opzionale) restano possibili senza breaking. | collaborative |
| D024 | M021 pre-S01 — co-decisa con D023, abilita preview UI/AI per hook fn | M021 — kernel-combat / skill API + UI/AI | Come consentire dry-run/preview di hook fn senza side-effect sulla queue live | **`SkillCtx::Mode { Live, DryRun }`** esplicito. In `Mode::Live`, `ctx.enqueue(Intent)` va alla queue del kernel. In `Mode::DryRun`, va a un buffer locale ritornato al chiamante; nessuna mutazione live. L'autore scrive **una sola** `resolve(...)` e gli **stessi** hook fn (D023); UI e AI ottengono preview reale eseguendo `Skill::resolve` in `Mode::DryRun`. `query_skill_preview` (D004) diventa thin wrapper. Sticky `Reject` (D003) funziona identico in entrambi i mode. | Senza `Mode::DryRun` esplicito, preview UI/AI non può girare hook fn (`ctx.enqueue` inquinerebbe la coda real-cast). Con due mode l'invariante `preview ≡ execute` di D009 diventa strutturale, non aspirazionale. Alternativa "una `predict_damage()` separata per UI" è la duplicazione che M021 elimina (D004). Costo zero a regime (branch predizione perfetta su mode costante). | Marginalmente; un terzo mode `Simulate { fork_state }` per AI lookahead profondo è additivo. Drop di `DryRun` dopo che UI lo usa sarebbe breaking. | collaborative |
| D025 | M021 pre-S01 — spike pass-1/2 conferma timeline FSM come backbone esecuzione | M021 — kernel-combat / skill execution model | Forma del modello di esecuzione skill — monolitico `apply_effects()` vs FSM esplicita | **Timeline FSM esplicita load-bearing.** Ogni skill compila a `CompiledTimeline = { beats: Vec<Beat>, edges: Vec<BeatEdge> }` valutata da un `BeatRunner` deterministico. `BeatKind = { CastStart, Impact, Loop { body, exit_when }, CastEnd }`. `BeatEdge = { from, to, predicate: Option<PredicateId> }`. Il runner avanza beat-by-beat emettendo `BeatEvent { kind, beat_id, hop_index }`, valutando edges in ordine (D029). `apply_effects()` e `enum Effect` monolitici eliminati a fine S-A5 (D015 Q5). | Modello monolitico `match Effect { ... } => apply()` forzava ogni nuova meccanica (Loop, hop, branch condizionale, telegraph) a estendere l'enum + un match nel kernel — esattamente l'anti-pattern P001. FSM esplicita disaccoppia struttura (graph data, autorizzata in RON) da semantica (kernel valuta graph). Validato dallo spike pass-1/2: 22/22 test verde inclusa Bounce come `BeatKind::Loop` (no kernel changes oltre il runner). Pattern Star Rail / Aethermancer: skill come timeline animation-driven con beat → impact → side-effect. | No (load-bearing). Cambiare il modello post-M021 invaliderebbe ogni timeline scritta. Estensioni additive: nuove varianti `BeatKind` (es. `Telegraph`, `Channel`) senza breaking. | collaborative |
| D026 | M021 pre-S01 — spike pass-2 valida due tempi separati | M021 — kernel-combat / timing model | Come riconciliare tempo deterministico (test/replay) con tempo di animazione (Windowed UI) | **Two-clock model load-bearing.** (1) `HeadlessAuto` clock: ogni beat si risolve istantaneamente; nessun mapping a wall-clock; usato da test, replay, server-side resolution. (2) `Windowed` clock: ogni beat ha `CueDef { duration_ms, anim_id, audio_id }` consumato da un dispatcher UI che impone delay reali. **Invariante load-bearing**: lo stream di `Intent` emesso dal kernel è **identico bit-per-bit** tra i due clock — solo il tempo wall-clock differisce. UI legge eventi dal bus, mai dallo stato; il bus emette stesso ordine indipendentemente dal clock. v0 cue duration: stub fisso `200ms/cue` con TODO asset pipeline (M022/M023). | Single-clock model (UI guida tempo, headless istantaneo) violerebbe determinismo: timing UI-dipendente farebbe diverge replay headless vs windowed. Two-clock con stream-invariant garantisce replay perfetto da JSONL e UI smooth indipendentemente. Pattern standard nei game engine con bus-event (UI è reader, mai source of truth). | Sì; clock aggiuntivi (es. `SuperFastForward` per AI training) sono additivi. L'invariante stream identico è invece load-bearing. | collaborative |
| D027 | M021 pre-S01 — proposta iniziale per talenti skilltree-driven; **demoted** post pass-3 | M021 — kernel-combat / skill mutation | `TimelinePatchOp` compile-time per riscrivere topologia timeline in base a skilltree | **DEMOTED.** Originale: skilltree con `patch_ops: Vec<TimelinePatchOp>` (Insert/Remove/Replace beat/edge) applicate al `CompiledTimeline` a load-time. Sostituita da: predicate runtime gate su `BeatEdge.predicate` che legge `ctx.skilltree()` (D033). Esempio: Bouncing Fire diventa edge condizionale `if ctx.skilltree().has("agumon.bouncing_fire") → enter Loop`. Validato dallo spike pass-3 (F11: `bouncing_fire_off_baseline_identical_to_no_loop`). | Compile-time patch creava due rappresentazioni della stessa skill (con/senza talent), raddoppiava la superficie di test, e rendeva non-osservabile dal JSONL il gating ("la timeline emessa è la `with_bouncing_fire`" vs "la baseline + gate-on-edge"). Runtime gate centralizza la decisione: una sola `CompiledTimeline` canonica, predicate fn-by-id sceglie il path. Costo: una valutazione predicate per edge candidate. D027 resta **available if needed** per casi futuri di topology rewrite non rappresentabile come edge-gate (es. inserzione di un beat con dipendenze cross-talent). | Sì; ripromuovere D027 è additivo (capability su lato compiler) se emerge un caso reale. v0 non ne ha. | collaborative |
| D028 | M021 pre-S01 — spike pass-2/3 segna signal taxonomy come chiusa | M021 — kernel-combat / signal bus | Forma del SignalBus — open string vs enum chiuso vs typed bag | **Enum chiuso `Signal` + validation a `App::finish()`.** Es. `enum Signal { OnHit, OnKill, OnAllyDown, OnTurnStart, OnUnitDied, OnDamageDealt, ... }`. `PassiveRunner` (D033) e `BlueprintListener` (D009) subscribono su variant specifici. Add di nuovo signal = aggiungere variant + match exhaustive forza ogni listener a dichiarare comportamento (default `()`). Validation at finish: cross-check che ogni signal subscribed esista come variant; nessun signal "orfano". | Open string bus invita typo non-debuggabili (es. `"on_hit"` vs `"OnHit"`); enum chiuso forza il compile-time check. Typed bag (`Box<dyn Any>`) sposta runtime cost senza valore aggiunto per la palette finita di signal (~15-20 nel roster). Coerente con D006: opaque dove serve estensibilità franchise (BlueprintSignal), enum chiuso dove il kernel ha il vocabolario fisso. | Sì; nuovi signal sono additivi (variant aggiuntivi). Drop di signal usati da listener attivi è breaking. | collaborative |
| D029 | M021 pre-S01 — spike pass-1/2 fissa semantica edge dispatch | M021 — kernel-combat / FSM dispatch | Ordine valutazione `BeatEdge` quando più edge partono dallo stesso beat | **First-passing edge + fallback unconditional invariant.** Il `BeatRunner` valuta gli edge nell'ordine di registrazione; il **primo** che soddisfa `predicate(beat_event, ctx) == true` viene preso; gli altri ignorati. **Invariante**: ogni beat con outgoing edges deve avere **almeno un edge con `predicate: None`** (fallback unconditional) come ultima entry, altrimenti `validate_timeline_refs` panica a load. Garantisce: nessun stuck-state, nessuna ambiguità, ordinamento esplicito tra branch. | Alternativa "valuta tutti, errore se più di uno passa" forzava l'autore a scrivere predicate mutuamente esclusive — rumoroso e fragile. First-passing rende esplicito l'ordering di precedenza (es. "se has_pyromaniac then loop, altrimenti single-hit") senza richiedere `not_X && not_Y` su ogni alternativa. Fallback obbligatorio elimina classe di bug "timeline stuck quando tutti i predicate falliscono". | No (load-bearing per author DX). Cambiare a "evaluate-all" invaliderebbe l'ordinamento implicito di edge usato dalle timeline scritte. | collaborative |
| D030 | M021 pre-S01 — mirror di D023, applicato a target selection | M021 — kernel-combat / target selection | Forma di `TargetSelector` (selector logica multi-target / bounce / chain) — enum vs fn pointer registry | **Selector fn-by-id registrato in `Registry<SelectorExt>`.** Signature: `fn(&SkillCtx, &SelectorInputs) -> Vec<UnitId>`. Niente enum chiuso per i selector custom (es. `bounce_pick_next`, `adj_lowest`, `lowest_hp_alive`). Built-in selector primitivi (Single, AllAllies, AllEnemies, RandomEnemy) restano enum core in `TargetShape`; tutti i selector "smart" passano da fn-by-id. Mirror simmetrico di D023 per gli hook. | Selector con logiche custom (Bounce con cast_hit_set, identity-filter cross-blueprint, chain-with-skip) sarebbero enum bloat impossibili da estendere senza editare il kernel. Pattern fn-by-id mantiene il kernel invariante: aggiungere `select_twin_core_partner` è una linea in `register()` del blueprint, zero kernel edit. Validato dallo spike: 4 selector custom registrati per 4 pattern (Bouncing Fire, Twin Core, Predator Loop, Battery Loop). | No (load-bearing per blueprint authors). Built-in core enum restano per il fast-path AoE/Single. | collaborative |
| D031 | M021 pre-S01 — generalizzazione D023 + D030 + altri 5 assi | M021 — kernel-combat / extensibility framework | Pattern unificato per ogni extension point fn-by-id (hook, selector, predicate, formula, tick, ai_utility, cue) | **Singolo `Registry<E: ExtPoint>` parametrico, 7 instance.** Trait `trait ExtPoint { type Fn: Copy; type Id: Copy + Eq + Hash + 'static; }`. `pub struct Registry<E: ExtPoint>(HashMap<E::Id, E::Fn>)`. 7 instance: `HookExt`, `PredicateExt`, `SelectorExt`, `FormulaExt`, `TickExt`, `AiUtilityExt`, `CueExt`. Container unico `ExtRegistries { hooks, predicates, selectors, formulas, ticks, ai_utilities, cues }` = singolo `Resource` startup-frozen. Niente `Box<dyn Fn>`, niente DSL parser, niente WASM/scripting embedded. Niente macro-table autogenerata (v0). | Pattern unificato evita 7 implementazioni divergenti del "registry fn-by-id"; semplifica l'API surface (un singolo `reg.register::<HookExt>(id, fn)` da imparare); zero allocazioni per dispatch (fn pointer Copy); type-safety completa via associated type. `Box<dyn Fn>` rifiutato: allocazione + indirezione senza valore (le funzioni non capturano stato — lo stato è in `SkillCtx`). DSL/WASM rifiutati: complessità sproporzionata per v0 (6 Digimon). | Sì; futuri assi (es. `RewardExt` per shop drops) sono additivi (nuova instance). Dropping di un asse richiederebbe rimozione di tutti i blueprint che lo usano. | collaborative |
| D032 | M021 pre-S01 — supersede D007 in chiave fn-by-id | M021 — kernel-combat / blueprint structure | Forma di un blueprint Digimon — trait `Blueprint + Box<dyn>` vs modulo + `register()` fn | **Un modulo + un `pub fn register(reg: &mut ExtRegistries)` per Digimon.** Path: `src/combat/blueprints/<name>/{mod.rs, identity.rs, hooks.rs, signals.rs}`. La `register()` installa tutto: hook, predicate, selector, formula, tick, ai_utility, cue, timeline canon per quel Digimon. Nessun `trait Blueprint`, nessun `Box<dyn Blueprint>`, nessun `BlueprintRegistry: Resource<HashMap<owner, Box<dyn Blueprint>>>` separato (D007 superseded). Identity statica via `pub const IDENTITY: BlueprintIdentity = BlueprintIdentity { id, name, attribute, ... }` esposta dal modulo per validation/UI/RosterEntry. State mutabile per-Digimon (Predator Loop counter, Twin Core stack) vive in `ExtRegistries.blueprint_state: HashMap<(UnitId, &'static str), i32>` (D034 ne è il write-path). | Trait + `Box<dyn>` aggiungeva indirezione e allocazione per zero valore aggiunto: ogni blueprint conosce staticamente il proprio set di fn da registrare. Modulo + fn = pattern Rust idiomatico minimo (mirror Bevy `Plugin::build`). Test "aggiungere un nuovo Digimon" = creare modulo + chiamare `register()` in startup — 5 righe boilerplate al ricetta unica. | No (load-bearing per la struttura blueprint). Reintroducendo trait richiederebbe riscrivere ogni `register()`. | collaborative |
| D033 | M021 pre-S01 — load-bearing post spike pass-2/3 | M021 — kernel-combat / runtime context | Skilltree mutability + Loop FSM beat + hop_index location | **(1) Skilltree immutable per la durata del run** (Slay-the-Spire pattern). `SkillTree { unlocked: HashSet<TalentId>, ranks: HashMap<TalentId, u8> }` come Component per-unit, **mai mutata in-cast**; sblocchi avvengono tra battaglie. Letta da predicate fn via `ctx.skilltree(unit)`. **(2) `BeatKind::Loop { body: Vec<BeatId>, exit_when: PredicateId }`** come variant nativa: il runner esegue `body` ripetutamente fino a quando `exit_when` ritorna true; ogni iterazione incrementa `hop_index`. Single-level nesting v0 (`debug_assert!(loop_depth <= 1)` + validation error). **(3) `hop_index: u32` su `BeatEvent` (opzione A)**: campo uniforme, `0` fuori Loop, `0..N` dentro. Mantiene firma unica `fn(&BeatEvent, &SkillCtx) -> bool` per tutti i predicate; nessun raddoppio di asse Registry. Type-safety persa è inerte (nessun predicate non-loop ha motivo di leggere hop_index). | Skilltree mutabile in-cast richiederebbe `Intent::SetSkillTree` + replay con stato skilltree time-varying = costo certo per benefit zero (nessuna skill canon ha "talent che cambia in-cast"). Immutable garantisce predicate determinismo e replay perfetto. `BeatKind::Loop` nativa cattura il pattern Bounce/multi-hit senza richiedere a ogni blueprint di reinventare un loop unrolled — validato dallo spike (Bouncing Fire, Predator Loop, Battery Loop, Tohakken). hop_index opzione A invece di raddoppiare l'asse Registry (B = `fn_with_hop` vs `fn_no_hop`) tiene singola la firma a costo di un campo dead nei predicate non-loop — trade vinto dalla semplicità autoriale. | Marginalmente: skilltree mutabile post-MVP è additivo (aggiungere `Intent::SetSkillTree` + invalidation). Multi-level Loop nesting è additivo (rimuovere assert). Cambio location di hop_index a registry duale sarebbe breaking. | collaborative |
| D034 | M021 post spike pass-3 — write-path canonico per blueprint state mutabile | M021 — kernel-combat / blueprint state mutation | Canonical write-path per la mutazione di state Digimon-specifico (`blueprint_state: HashMap<(UnitId, &'static str), i32>`) durante una cast | **`Intent::SetBlueprintState { unit, key, value }` come unico canale di mutazione.** Le hook fn leggono `ctx.blueprint_state(unit, key) -> i32` (default 0) e scrivono esclusivamente via `ctx.enqueue(Intent::SetBlueprintState { ... })`. Nessuna mutazione diretta del HashMap dalle fn. Payload v0: `value: i32` semplice (non varianti tipizzate per key) — sufficiente per il roster (Predator Loop counter, Twin Core heat stack, Battery Loop charge). Aggregator centralizzato lato `intent_applier` (D008): la mutation finisce sia nel transition stream osservabile sia nel HashMap per la prossima read. | Read via accessor + write via Intent è il pattern già consolidato per ogni altra mutazione (damage, heal, status). Mutare il HashMap direttamente dalle hook fn romperebbe l'invariante "tutte le mutazioni passano dal transition stream" (D008), rendendo non-replicabile lo stato per replay/forensics. `i32` generico evita di forzare le hook a conoscere uno schema chiuso di key (anti-coupling kernel ↔ blueprint, P001). Spike F12 valida che read-via-predicate + write-via-Intent compongono naturalmente. Alternativa "key-tipizzate (enum)" rifiutata perché ricrea coupling che P001 elimina; alternativa "mutazione diretta" rifiutata perché perde observability. | Sì; aggiungere varianti `Intent::IncBlueprintState`, `Intent::AddBlueprintState` etc. è additivo. Sostituire `i32` con `enum Value { Int(i32), Bool(bool), ... }` è additivo. Promuovere a key-tipizzate è breaking. | collaborative |
| D035 | M021 post spike pass-2/3 — superseding di D007 e D012 per riflettere il design fn-by-id validato | M021 — kernel-combat / blueprint structure | Lifecycle del `BlueprintRegistry` (Resource HashMap owner → Box<dyn Blueprint>) come definito in D007 — ancora load-bearing post spike pass 2/3? | **D007 superseded de facto da D032.** Lo spike pass 2/3 ha rimosso il `trait Blueprint` e il `BlueprintRegistry: Resource<HashMap<owner, Box<dyn Blueprint>>>` dal critical path. Sostituito da: ogni Digimon è un modulo `src/combat/blueprints/<x>/` con una fn `register(reg: &mut ExtRegistries)` che installa hook/predicate/selector/formula/tick/ai/cue/timeline sulle 7 registry parametriche (D031). Nessuna struct `Box<dyn Blueprint>` allocata, nessun `BlueprintRegistry` separato. `ExtRegistries` è il singolo `Resource` Bevy registrato a startup. La domanda "startup-frozen vs single-writer mutable" di D007 non si applica più — `ExtRegistries` è frozen by construction. Hot-reload/DLC dinamico restano deferred come da D007. D012 (type-level enforcement blueprint↔state via `register_blueprint::<B: Blueprint>`) decade conseguentemente — sostituito da: ogni `register()` per-Digimon installa direttamente lo state se serve (D034 ne è il write-path). | D007 cristallizzava la forma "trait Blueprint + Box<dyn> registry". Lo spike ha mostrato che il pattern fn-by-id (D023 hook fn + D030 selector fn + D031 Registry<E>) è strutturalmente più semplice e copre lo stesso set di responsabilità senza trait objects, allocazioni, o registry separato. Le risposte di D007 sul lifecycle (startup-frozen, `pub(crate) register`, no hot-reload) restano corrette ma applicate al nuovo container (`ExtRegistries`). Formalizzo la supersessione qui per audit-trail invece di editare D007 in-place (rispetta append-only). | No, è una supersessione definitiva: ripristinare `trait Blueprint + Box<dyn>` post-spike richiederebbe evidence che il pattern fn-by-id ha un limite reale (nessuno emerso in 33/33 test verde sui 4 pattern stress-testati). Hot-reload futuro è additivo su `ExtRegistries` (Resource mutabile + invalidation) come era additivo su `BlueprintRegistry`. | collaborative |
