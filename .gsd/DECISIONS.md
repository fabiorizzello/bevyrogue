# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M021/S07/T04 | combat | Emit `BlockReactionTriggered` whenever any one-shot passive mitigation modifier is consumed in the incoming-damage pipeline, not only on Tentomon's block proc. | Shared event as the canonical pre-damage-mitigation seam; Tentomon block + generic passive modifiers funnel through it. | One observable seam for all passive mitigation; keeps damage pipeline + Tentomon aligned; preserves the generic block-reaction test. | Yes | agent |
| D002 |  | architecture | How TwinCore transitions route through CombatKernelTransition after removing the digimon-specific variant. | Replace `CombatKernelTransition::TwinCore(..)` with `Blueprint { owner:"twin_core", name, payload:Amount }`; `apply_twin_core_transitions_system` decodes owner=="twin_core" → TwinCoreSignal in the twin_core module. | M021 M5 mandates removing 5 digimon variants from the kernel; TwinCore is blueprint-level, not a kernel primitive. Blueprint envelope keeps kernel generic, preserves JSONL observability. | Yes | agent |
| D003 | M021/S10 planning | combat | How shared event/observability surfaces represent digimon-specific runtime resolution after S10 removes digimon names from shared kernel files. | Remove digimon-named resolved variants from shared `CombatEventKind`/observability; owner-gated blueprint runtime state/snapshots become the canonical diagnostic seam; shared surfaces expose only generic Blueprint envelopes. | S10 exit proof: src/combat outside blueprints must be digimon-free. Blueprint-owned state already carries mechanic detail, so moving the contract behind owner resources keeps determinism while shared code stays generic. | Yes | agent |
| D004 | M021/S11 planning | architecture | How S11 world-backed preview consumers integrate with existing UI and enemy-turn systems. | Keep `query_skill_preview` as a shared world-backed helper; consume via narrow exclusive bridge systems/resources (UI preview cache, enemy-AI decision bridge); don't convert `combat_panel`/`advance_turn_system` to exclusive wholesale. | Both consumers need `&mut World` for `BeatRunner` Preview mode; narrow bridges preserve headless-first structure, minimize schedule churn, share one preview contract. | Yes | agent |
| D005 | M021/S12 planning | architecture | How S12 removes digimon-named roster metadata and validation snapshot fields. | Adopt a generic owner-keyed `UnitDef` blueprint payload + a `ValidationExt` registry axis; scope S12 to schema/validation decoupling only. | Hardcoded `twin_core`/`holy_support` roster fields + named validation substructures are the load-bearing seams; generic owner-keyed payload + registry contributors satisfy C2/C3 without another shared per-digimon enum. | Yes | agent |
| D006 | M021 fascia B §8 Q-B2 | M021 — kernel-combat / blueprint extensibility | Type of the `Intent::BlueprintSignal` payload — opaque vs closed enum. | Keep payloads opaque to the kernel with owner-scoped downcast helpers in the owning mechanic. | A closed blueprint enum recreates the coupling M021 removes; opaque owner-scoped payloads keep the kernel generic. | Yes | collaborative |
| D007 | M021 fascia B §8 Q-B3 | M021 — kernel-combat / blueprint extensibility | Lifecycle of the BlueprintRegistry / registration container. | Startup-frozen registry; registration at startup only, no hot-reload in M021. | No dynamic registration needed; mutable hot-reload adds invalidation cost for little value. Later superseded in impl by fn-by-id `register()` (kept for trail). | Yes | collaborative |
| D008 | M021 pre-roadmap Q7 | M021 — kernel-combat / observability | How BlueprintSignal enters the transition stream. | Route `Intent::BlueprintSignal` through the kernel transition stream first, then dispatch to the owning runtime. | Bypassing the stream drops blueprint state from JSONL and breaks replay/forensics; the extra hop keeps relevant mutations observable. | Yes | collaborative |
| D009 | M021 pre-S01 | M021 — kernel-combat / skill API | Historical D023 — Shape of chain hooks (`on_cast`/`on_hit`/`on_kill`/`on_miss`/`on_final_hop`). | Rust fns registered by ID, not a declarative OnHit/OnKill enum. | Fn-by-ID kills enum bloat, keeps kernel closed, unifies with passive/listener model; observable contract is the Intent/CombatEvent stream. | No | collaborative |
| D010 | M021 pre-S01 | M021 — kernel-combat / skill API + UI/AI | Historical D024 — Dry-run/preview support for hook functions. | Explicit `SkillCtx::Mode { Live, DryRun }` so one resolve path powers execute + preview. | Without it, preview mutates live queues or duplicates a prediction path; a mode switch keeps `preview ≡ execute` while containing side effects. | Yes | collaborative |
| D011 | M021 pre-S01 | M021 — kernel-combat / skill execution model | Historical D025 — Explicit timeline FSM vs monolithic `apply_effects()`. | Timeline FSM is load-bearing: skills compile to beats/edges, a deterministic BeatRunner advances them. | A monolithic effect matcher forces every mechanic to extend kernel enums; FSM keeps structure in data, semantics in shared runtime. | No | collaborative |
| D012 | M021 pre-S01 | M021 — kernel-combat / timing model | Historical D026 — Deterministic kernel clock vs windowed presentation clock. | Two-clock model: headless resolves immediately, windowed attaches presentation timing, emitted Intent stream identical. | UI timing must not alter the runtime stream or replay/tests diverge; two clocks preserve determinism with UI pacing. | Yes | collaborative |
| D013 | M021 pre-S01 | M021 — kernel-combat / skill mutation | Historical D027 — Demote `TimelinePatchOp` in favor of runtime edge gating. | No compile-time `TimelinePatchOp` by default; one canonical timeline, optional branches gated by runtime predicates. | Compile-time patching duplicates skill representations and hides gating; runtime predicates keep one timeline, visible/testable. | Yes | collaborative |
| D014 | M021 pre-S01 | M021 — kernel-combat / signal bus | Historical D028 — Closed signal bus vocabulary. | Closed, validated signal vocabulary — fixed taxonomy, not free-form strings. | Open string buses invite typo failures; closed surface keeps startup-time validation, extensibility lives in blueprint payloads. | Yes | collaborative |
| D015 | M021 pre-S01 | M021 — kernel-combat / FSM dispatch | Historical D029 — Beat edge dispatch semantics. | Evaluate outgoing beat edges in declaration order, take first passing; require a final unconditional fallback. | First-passing makes precedence explicit without mutually-exclusive predicates; fallback prevents stuck timelines. | No | collaborative |
| D016 | M021 pre-S01 | M021 — kernel-combat / target selection | Historical D030 — Target selector shape: enum primitives plus selector fn-by-id. | Primitive target shapes in core; all smart/custom selectors registered by ID. | Custom selectors (bounce, adjacency, owner-specific) shouldn't expand kernel enums; ID registry keeps kernel stable. | No | collaborative |
| D017 | M021 pre-S01 | M021 — kernel-combat / extensibility framework | Historical D031 — Unified fn-by-id extensibility registries. | One generic `Registry<E>` across extension axes, grouped under one `ExtRegistries`. | Avoids seven drifting impls; fn pointers suffice — trait objects/DSLs add cost without value. | Yes | collaborative |
| D018 | M021 pre-S01 | M021 — kernel-combat / blueprint structure | Historical D032 — Blueprint module plus `register()` function. | Each Digimon = a module with `register(reg:&mut ExtRegistries)`, not a trait-object instance. | Module+register keeps wiring static, simple, allocation-free; matches fn-by-id design over `Box<dyn Blueprint>`. | No | collaborative |
| D019 | M021 pre-S01 | M021 — kernel-combat / runtime context | Historical D033 — Immutable skilltree plus native Loop beats and `hop_index`. | Immutable skilltree during a run; model bounce/repeat with native Loop beats; carry `hop_index` on BeatEvent. | Immutable skilltree preserves replay; Loop beats capture repeats without kernel special cases; one predicate signature. | Yes | collaborative |
| D020 | M021 post spike pass-3 | M021 — kernel-combat / blueprint state mutation | Historical D034 — Canonical mutation path for blueprint state. | `Intent::SetBlueprintState` is the canonical write path for blueprint-owned mutable state. | Direct hook mutation bypasses the transition/event stream and breaks replay/observability. | Yes | collaborative |
| D021 | M021 post spike pass-2/3 | M021 — kernel-combat / blueprint structure | Historical D035 — Supersede the trait-object BlueprintRegistry design. | `BlueprintRegistry<Box<dyn Blueprint>>` superseded by fn-by-id `register()` + `ExtRegistries`. | Spike showed fn-by-id covers the same responsibilities with less indirection/machinery. | No | collaborative |
| D022 | Post-M016 refactoring and cleanup | architecture | How to enforce decoupling between the generic Combat Kernel and specific Digimon Blueprints. | `src/combat/kernel.rs` must never import/reference/register specific Digimon blueprints; registration happens at the composition layer (`src/combat/mod.rs` `BlueprintsPlugin`) + per-test setup. | Kernel hardcoding digimon plugins violates "kernel chiuso, blueprint aperti"; adding/removing a Digimon must never touch the generic kernel. | Yes | agent |
| D023 | Post-M016 design cleanup and M021 spike evaluation | architecture | How to decouple the generic FSM/Kernel from custom Digimon mechanics without centralized registries or specific kernel transitions. | Generic kernel uses a generic `KernelEffect` bus (`DealDamage`/`ApplyStatus`/`GrantSp`) + generic `CombatEvent`s; NO centralized string-ID registries (`ExtRegistries<ExtPoint>`) or digimon-specific kernel transitions; all specific logic in the Digimon's own Plugin emitting generic effects. | Earlier prototypes added digimon kernel variants / string-ID registries, violating "closed kernel, open blueprint"; plugins reacting/emitting generic data keep the kernel unaware of any Digimon. | Yes | agent |
| D024 |  | sprite-pipeline | Whether to repack/auto-crop sprite atlases to per-character bounding boxes. | Remove repack_atlas.py — all sprites use uniform 512px frames, no per-character cropping. | Uniform dimensions simplify Bevy atlas loading + consistent scale; per-character crop produced varying cell sizes. | no | human |
