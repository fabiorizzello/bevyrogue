# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M021/S07/T04 | combat | Emit `BlockReactionTriggered` whenever a one-shot passive mitigation modifier is consumed in the incoming-damage pipeline, not only when Tentomon's reactive block proc succeeds. | Use the shared event as the canonical diagnostic surface for any pre-damage passive mitigation, with Tentomon's block reaction and generic passive damage modifiers both funneled through the same event. | This keeps the damage pipeline and the Tentomon passive aligned, preserves the existing generic block-reaction test, and gives one observable seam for future passive mitigation effects. | Yes | agent |
| D002 |  | architecture | How TwinCore transitions route through CombatKernelTransition after removing the digimon-specific variant | Replace `CombatKernelTransition::TwinCore(TwinCoreTransition)` with `CombatKernelTransition::Blueprint { owner: "twin_core", name: "<signal_name>", payload: SignalPayload::Amount(amount) }`. The `apply_twin_core_transitions_system` decodes Blueprint events where owner=="twin_core" and maps name to the corresponding TwinCoreSignal internally within the twin_core module. | M021 CONTEXT M5 mandates removal of 5 digimon-specific variants from CombatKernelTransition, replaced by a generic Blueprint variant. TwinCore is shared between Agumon and Gabumon but is still a blueprint-level mechanic, not a kernel primitive. Routing through Blueprint { owner: "twin_core" } keeps the kernel generic while preserving full observability via JSONL (same event shape as passive triggers from S07). | Yes | agent |
| D003 | M021/S10 planning | combat | How shared event and observability surfaces should represent digimon-specific runtime resolution after S10 removes digimon names from shared kernel files | Remove digimon-named resolved variants from shared CombatEventKind and shared observability structs; use owner-gated blueprint runtime state/snapshots as the canonical diagnostic seam, with shared surfaces exposing only generic Blueprint envelopes and generic formatting/aggregation. | S10's exit proof requires src/combat outside blueprints to be digimon-free, which conflicts with keeping BatteryLoopResolved, PredatorLoopResolved, holy_support, and precision_mind_game named seams in events.rs and observability.rs. Blueprint-owned state already carries the mechanic-specific detail, so moving the diagnostic contract behind owner-owned resources preserves determinism while allowing shared kernel and observability code to stay generic. | Yes | agent |
| D004 | M021/S11 planning | architecture | How S11 world-backed preview consumers should integrate with existing UI and enemy-turn systems | Keep `query_skill_preview` as a shared world-backed helper, then consume it through narrow exclusive bridge systems/resources (UI preview cache and enemy-AI decision bridge) instead of converting `combat_panel` or `advance_turn_system` wholesale into exclusive systems. | Both consumers need `&mut World` to run `BeatRunner` in `SkillCtxMode::Preview`, but the current egui panel and turn-advance systems are typed Bevy systems with broad existing call sites and tests. Narrow bridge systems preserve headless-first structure, minimize schedule churn, and let UI/AI share the same preview contract without duplicating timeline execution logic. | Yes | agent |
| D005 | M021/S12 planning | architecture | How S12 should remove digimon-named roster metadata and validation snapshot fields | Adopt a generic owner-keyed `UnitDef` blueprint payload plus a new `ValidationExt` registry axis, and keep S12 scoped to schema/validation decoupling rather than broader plugin-registration or asset-layout automation. | Research and code validation show the current hardcoded `twin_core`/`holy_support` roster fields and named validation snapshot substructures are the load-bearing shared seams for S12, while the stronger 'new digimon touches only 2 dirs' proof is additionally blocked by central blueprint registration and the monolithic `assets/data/units.ron` loader. A generic owner-keyed payload and registry-driven validation contributors satisfy milestone context C2/C3 without introducing another shared per-digimon enum, and they keep deterministic serialization/formatting feasible. | Yes | agent |
