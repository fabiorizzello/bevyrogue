# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M021/S07 | combat | BlockReactionTriggered trigger scope | Shared event for any 1-shot mitigation (Tentomon + passives) | One observability seam for all pre-damage mitigation | yes | agent |
| D002 | M021 | arch | TwinCore transition routing | Blueprint { owner:"twin_core", name, payload } | Kernel generic; twin_core module decodes its own state | yes | agent |
| D003 | M021/S10 | combat | Digimon-named seams in shared code | Remove named variants; use owner-gated snapshots/blueprint | S10 exit: src/combat must be digimon-free. Generic envelopes. | yes | agent |
| D004 | M021/S11 | arch | World-backed preview integration | Narrow bridge systems (UI/AI preview cache) | Both need &mut World; bridges minimize churn, share contract | yes | agent |
| D005 | M021/S12 | arch | Digimon-named roster/validation fields | Owner-keyed UnitDef payload + ValidationExt registry | Decouples schema; registry contributors satisfy C2/C3 | yes | agent |
| D006 | M021 | blueprint | BlueprintSignal payload type | Opaque payload with owner-scoped downcast | Closed enum recreates coupling; opaque keeps kernel generic | yes | collaborative |
| D007 | M021 | blueprint | BlueprintRegistry lifecycle | Startup-frozen; no hot-reload in M021 | No dynamic need; mutable registration adds invalidation cost | yes | collaborative |
| D008 | M021 | observ. | BlueprintSignal routing | Route through kernel transition stream first | Bypassing drops state from JSONL; hop keeps mutations observable | yes | collaborative |
| D009 | M021 | skill | Chain hook shape (D023) | Rust fns by ID, not declarative enum | Kills bloat; kernel stays closed; unified with passives | no | collaborative |
| D010 | M021 | skill | Preview support for hooks (D024) | SkillCtx::Mode { Live, DryRun } | Mode switch keeps preview ≡ execute; contains side-effects | yes | collaborative |
| D011 | M021 | skill | Execution model (D025) | Timeline FSM (beats/edges) + BeatRunner | Monolith forces kernel enum expansion; FSM keeps semantics data | no | collaborative |
| D012 | M021 | timing | Clock model (D026) | 2-clock: Headless resolution vs Windowed presentation | UI timing must not alter stream; determinism + presentation | yes | collaborative |
| D013 | M021 | skill | Skill mutation (D027) | Runtime edge gating over TimelinePatchOp | Patching hides gating; predicates keep one visible timeline | yes | collaborative |
| D014 | M021 | signals | Signal bus (D028) | Closed taxonomy, no free-form strings | Open strings invite typos; closed bus allows boot validation | yes | collaborative |
| D015 | M021 | kernel | Beat edge dispatch (D029) | Declaration order + first-passing + mandatory fallback | Precedence explicit; fallback prevents stuck timelines | no | collaborative |
| D016 | M021 | combat | Target selector shape (D030) | Primitives in core; custom selectors by ID | Custom bounce/adjacency shouldn't expand kernel enums | no | collaborative |
| D017 | M021 | kernel | Extensibility framework (D031) | Unified Registry&lt;E&gt; under ExtRegistries | Avoids drifting impls; fn pointers suffice over trait objects | yes | collaborative |
| D018 | M021 | blueprint | Blueprint structure (D032) | Module with register(reg), no trait instances | Wiring static/simple/allocation-free; matches fn-by-id | no | collaborative |
| D019 | M021 | kernel | Loop semantics (D022) | Native Loop beats + hop_index on BeatEvent | Immutable skilltree; Loop beats capture repeats natively | yes | collaborative |
| D020 | M021 | blueprint | Blueprint state mutation (D034) | Intent::SetBlueprintState is canonical path | Direct mutation bypasses transition stream and observability | yes | collaborative |
| D021 | M021 | blueprint | Blueprint registry redesign (D035) | Supersede trait-objects with fn-by-id + ExtRegistries | Fn-by-id covers duties with less indirection | no | collaborative |
| D022 | M016 | arch | Kernel-Blueprint decoupling | src/combat/kernel.rs never imports blueprints | Registration at composition layer (BlueprintsPlugin) | yes | agent |
| D023 | M021 | arch | Custom mechanic decoupling | KernelEffect bus + CombatEvents; NO ExtRegistries | Reacting/emitting generic data keeps kernel unaware of Digimon | yes | agent |
| D024 | sprites | sprites | Sprite atlas auto-crop | 512px uniform frames; no per-character cropping | Simpler atlas load; consistent scale across animations | no | human |
| D025 | M002 S02 planning | runtime/two-clock | S02 timeline cue barrier ownership | Represent manual presentation timing with a generic combat runtime resource (TimelineClock plus suspended timeline/cue state) and let windowed systems opt into Clock::Windowed and release the cue; keep headless/default execution on Clock::HeadlessAuto | The combat kernel remains feature-agnostic and deterministic, while windowed rendering controls only when a queued barrier is released. This avoids windowed dependencies in src/combat and keeps the I3 headless-vs-windowed intent stream contract testable without wgpu. | yes | agent |
| D026 | M002 S04 planning | combat/reactive-vfx | S04 Baby Burner detonate architecture | Implement Baby Burner detonate as a Rust-only post-application reaction registered under the Agumon blueprint, using an owner-neutral reaction seam and generic blueprint transition/intent outputs rather than AnimGraph KernelEvent branching, RON timeline migration, or beat-by-beat timeline commits | The current timeline runner buffers effects until finalization and AnimGraph KernelEvent predicates are not runtime-executable, so a Rust-side post-application seam proves the reactive contract headlessly while preserving deterministic two-clock behavior and keeping Digimon-specific logic out of shared combat code. | yes | agent |
| D027 | M002 S09 planning | observability/windowed-validation | S09 frame-time baseline measurement approach and regression threshold for the windowed soak | Add a pure, headless-testable frame-time aggregator (count/mean/min/max/p95) in the lib observability module; feed it real Time::delta_secs only from the windowed presentation tick; obtain the kernel-only baseline by a windowed soak run with the anim-graph/render systems disabled via a BEVYROGUE_VALIDATION_BASELINE env toggle (apples-to-apples, same presentation layer); emit a structured validation_frametime: log line mirroring the validation_snapshot: convention; pass bar = anim-graph-attributable mean regression <= 15% and p95 <= 20% vs baseline, with a 2ms absolute mean tolerance for low-sample noise. | R004 forbids wall-clock/RNG in the deterministic resolution stream, so measurement must stay in the Clock::Windowed presentation layer; a full-vs-baseline windowed comparison isolates anim-graph cost without comparing against an artificial headless fixed-timestep clock. A pure aggregator keeps the math deterministic and unit-provable headlessly even when no display is available for the live soak. | yes | agent |
| D028 | M003 planning | scope | What M003 actually delivers, given the GSD-DB title "Roster extension" is stale by one milestone | M003 is the pixel-binding milestone (bind the existing atlas, render all five Agumon surfaces on both actors); roster extension (Gabumon/Dorumon/Tentomon/Renamon/Patamon) slides to M004+. | M002 over-delivered the combat-presentation plumbing the portfolio reserved for its M003, leaving the on-screen actors textureless. The real next milestone is the visual reality-check the portfolio promised: making the proven-but-invisible stack render. Roster work depends on this exact binding path, so it must come first. | no — confirmed by M003-CONTEXT; the DB label is the only stale artifact | collaborative |
| D029 | M003 planning | architecture | M003 slice ordering and decomposition into three vertical slices | S01 atlas binding (idle + basic, riding the one already-bridged Sharp Claws path) → S02 Baby Flame + Baby Burner windowed cue bridges → S03 VFX flash as world particles. Binding-first, then the multi-timeline impact-frame sync risk, then the additive VFX surface. | Atlas binding has zero prior art in src/ and gates everything visible, so it is proven first and rides the only currently-bridged skill (Sharp Claws) to prove the full path end-to-end. Baby Flame and Baby Burner currently auto-release as 'unsupported windowed cue bridge' (render.rs:160) — wiring their cue bridges is the flagged impact-frame-sync-across-three-timelines risk, so it comes second once the render path exists. VFX-as-pixels is additive and accompanies the skill/ultimate surfaces, so it depends on both prior slices. | yes — slice boundaries may merge if S01 reveals the binding is trivial enough to fold basic/skill together | agent |
| D030 | M003 S01 planning | architecture | Where atlas-layout geometry lives, given the headless lib excludes bevy/2d (no TextureAtlasLayout) while tests under tests/ link only the lib | Add a Bevy-free geometry seam in the lib (src/animation/atlas.rs): AtlasGeometry::from_clip_meta(&ClipMeta) + an identity, total_frames-bounded atlas_index(frame)->Option<u32> map. The windowed binary (render.rs) constructs the real Bevy TextureAtlasLayout via TextureAtlasLayout::from_grid from that geometry; the Handle<Image>/TextureAtlasLayout/Image types stay entirely behind the windowed feature gate. | Cargo.toml enables only bevy_asset/bevy_log/bevy_state in the default (headless) feature set; bevy/2d (which provides TextureAtlasLayout and Image) is added solely by the windowed feature. Headless integration tests must exercise the frame→atlas-index contract, so the testable geometry must be a pure descriptor with no bevy/2d dependency. The identity map is correct because clip frame index equals atlas tile index, already proven by clip_atlas_parity. This keeps R016 hygiene (no windowed deps leak into headless paths) while making clip↔atlas parity and the impact-frame-on-rendered-frame invariant headless-provable. | yes — if a future Digimon needs a non-identity frame→index map (e.g. shared/packed atlas), AtlasGeometry can carry an explicit index table instead of identity | agent |
| D031 | Planning M003/S02 — extending the Sharp-Claws-only windowed cue bridge to Baby Flame (skill range) and Baby Burner (heavy_attack range). | data-authoring | How Baby Flame and Baby Burner barriers are released on the rendered frame given asymmetric skill authoring (S02) | Author ReleaseKernel cues as the uniform release trigger for every windowed-bridged barrier: (1) add presentation Some(cue_id agumon/baby_flame/impact, anim baby_flame_impact) to Baby Flame's impact_damage beat in skills.ron + a ReleaseKernel cue on the baby_flame_impact node; (2) add ReleaseKernel cues at the end-of-node local frame of baby_burner_charge (windup release) and baby_burner_recovery (recovery release) in anim_graph.ron (baby_burner_launch already has one). The windowed bridge releases the currently-awaiting barrier on the current node's ReleaseKernel local frame and fires fire_kernel_cue to drive the KernelCue-gated FSM transitions. | A single release mechanism (scan current node for a ReleaseKernel cue at the player's local frame) then handles all three skills uniformly. Asset-authored cues stay headless-testable from tests/ (which link lib-only per MEM030) via the established TC-6 RON-scan pattern, whereas an in-bridge end-of-node rule would only be testable as a render.rs #[cfg(test)] unit. It also makes the release contract explicit in data rather than hidden in windowed binary logic, and keeps the combat kernel feature-agnostic (MEM025): windowed only chooses when the queued barrier releases. | yes — if the end-of-node ReleaseKernel placement causes spurious animation holds, the bridge could fall back to an explicit TimeInNode-equivalent end-of-node release rule for windup/recovery only. | agent |
| D032 |  | architecture | How the inert SpawnParticle/detonate VFX seam becomes visible pixels in S03 | Mirror the S01 atlas split: a Bevy-free VfxSpawnDescriptor (built from &Command::SpawnParticle, carrying ParticleId/VfxLocus/VfxMotion + a renderable visual-component intent) plus a pure resolve_locus(locus, caster_xy, target_xy) helper live in the lib (src/animation/vfx.rs, pub use vfx::*); the windowed layer turns the descriptor into a short-lived colored Sprite quad entity (VfxParticle{ttl,motion}) with no new particle asset. Two spawn sources funnel through it: FSM on_enter SpawnParticle (Baby Flame cast) consumed in advance_agumon_presentation via node-entry detection, and the Baby Burner detonate signal consumed by a new windowed system reusing latest_baby_burner_flash_trigger. The BabyBurnerFlashState egui chip stays alongside. | The descriptor+resolver is the only CI-provable contract (lib-only link reaches it like AtlasGeometry), keeping bevy/2d behind the windowed feature (R002/R005). A colored quad satisfies "visible particles" without the ~100MiB/atlas VRAM cost flagged in CONTEXT. Funneling both spawn sources through one descriptor preserves the closed-enum, no-numeric-payload vfx_handle_seam parity (MEM044) and the cosmetic/deterministic boundary (CAP-7c065a44, R004: presentation-layer lerp on Time only, no physics/RNG/wall-clock). | yes — a real particle texture/atlas can back the same descriptor seam at roster scale without touching the lib contract | agent |
| D033 | M003 (pixel binding) complete; planning M004 to replace the hardcoded three-phase Baby Flame VFX polish (render.rs enum + kind_from_name string-match + per-skill offset fns) with the owned data-driven seam before the M004+ roster (Gabumon/Dorumon/Tentomon/Renamon/Patamon) multiplies the hardcoding. | architecture | VFX per-Digimon system architecture (the data-driven replacement for the hardcoded Baby Flame polish currently in src/windowed/render.rs) | Owned single per-Digimon vfx.ron on three orthogonal axes, no external particle dependency. (1) PLACEMENT — emitter motion (mouth-follow windup curve, homing/arc-to-target, converge-inward) owned by us as PlacementFn verbs registered in the existing Registry<E> (D031), resolved by namespaced string id; no closed match in the core. (2) APPEARANCE — spread/lifetime/scale+color keyframe curves/burst owned by us (~50 lines of pure curve-eval), reusable named appearance palette; NO bevy_enoki. (3) VARIATION — skill-tree-driven changes via variant SELECTION keyed by (skill_id, variant_key), same idiom as anim_graph (state picks which effect-tree to instantiate), with parametric `modifiers` (count/tint/size_mul/spread_mul) as sugar over selection. Chaining (charge→launch→impact) expressed in data via on_expire, not string-match in the engine. Deferred-additive (NOT built in M004): an expression-tree evaluator as one extra registered placement verb, and third-party runtime modding (scripting/WASM atop the registry by relaxing &'static str). All verb math stays pure and headless-testable (R004); only rendering is windowed-gated (R002/R005). | Surfaced via design-an-interface (4 designs A/B/C/D compared). The deciding lenses were the user's: a single future editor that unifies anim_graph + stance + particle, and R004 headless determinism. enoki (Designs B/D) was rejected because it owns only the APPEARANCE axis — the PLACEMENT axis (converge/homing/mouth-curve) is emitter-Transform motion that stays host-owned whether or not enoki is adopted — so enoki buys a second uncontrolled schema + a separate web editor + appearance escaping the headless test net, fragmenting exactly what the editor should unify, while saving only ~40-50 lines. Hosting verbs in Registry<E> (not a closed enum match) makes the engine extension-first: a new Digimon adds vfx.ron (content extension, no recompile); a novel motion adds register("ns/name", fn) in its blueprint (behavior extension, no core change) — same idiom as SelectorExt/PredicateExt and blueprints (D031/D032). Variant selection (not field-patching) was required because parametric modifiers alone cannot express structural variation (e.g. a skill-tree node turning one flame into three separately-homing chains); selection lets a variant be a wholly different on_expire chain. Expression-DSL and runtime modding are deferred because building them now is YAGNI with one Digimon on screen and risks R004; the registry is designed so both are additive later, not foundational rewrites. | Yes — APPEARANCE buy-vs-build (enoki adapter as Design-D port) and the depth of the variant/expression layer can be revisited if appearance iteration becomes painful or a node editor lands; both were designed as additive evolutions, not load-bearing now. | collaborative |
| D034 | M004 planning, immediately after D033; closing the editor-readiness question before writing the M004 roadmap | architecture | Whether the D033 per-Digimon VFX schema must be editor-ready (introspectable verb parameters) from M004 S01, and how — given the future goal of a single GUI editor that configures anim_graph + vfx together | Refine D033 with an editor-ready constraint: VFX verb parameters must be introspectable, NOT a stringly-typed params map. From M004 S01, each Placement/Appearance verb carries typed per-verb parameter structs (e.g. ConvergeInward { radius_px: f32, omega: f32 }) deriving Serialize + Deserialize (round-trip, already free) plus #[derive(Reflect)] so a future egui editor can generate parameter forms by reflection. Verb enumeration is already free via Registry::iter() (registry.rs:59). The two editor cardinals are thus both satisfied: (a) which verbs exist → Registry::iter; (b) which params each verb wants → typed structs + Reflect. The single-editor goal also requires adding #[derive(Reflect)] to the existing anim_graph.rs types (currently Serialize/Deserialize only) — additive, low-risk, scheduled as editor-milestone work, NOT in M004. The editor itself remains a separate milestone after M004; M004 only adopts the editor-ready param shape so the editor never forces a schema refactor. | Verified on code: anim_graph.rs types already derive Serialize+Deserialize (round-trip ready) and Registry exposes iter(), so verb enumeration is free. The one real gap for a data-driven editor is parameter introspection: a stringly-typed params map (radius_px/omega) hides param names/types/ranges inside the verb fn body, invisible to an editor — forcing per-verb hardcoded forms in the editor, i.e. re-introducing the exact hardcoding being removed, just relocated. Typed per-verb param structs + Reflect close this with the standard Bevy reflection path egui editors rely on, while deny_unknown_fields gives stronger load-time validation than a map. Chosen over the registry-manifest alternative (register carrying a param manifest) because typed structs are validated by the compiler, are the idiomatic match for Bevy reflection-driven UIs, and keep one source of truth per verb. Deciding now is required because it changes the S01 schema shape; deferring would force a schema refactor when the editor lands. | yes — if a node editor never materializes, the Reflect derives are harmless; if the registry-manifest approach proves better for runtime-registered third-party verbs, it can be added alongside typed structs since both resolve through the same Registry seam | collaborative |
| D035 | M004/S02 planning — placement verbs in Registry + generic render dispatcher | architecture | How typed placement-verb parameters are represented under D033's open string-id dispatch (the S02 Key Design Tension between D033 open registry and D034 typed params) | Closed `PlacementParams` enum in the headless lib (option A): `Placement { verb: String, params: PlacementParams, anchor: PlacementAnchor }`, where PlacementParams is a typed enum (ConvergeInward{radius_px,omega}, FanOut{spread_px}, ArcLaunch{}, Static) deriving Serialize+Deserialize+Reflect. Verb fns receive `&PlacementParams`. Verb RESOLUTION stays fully open (string id via `Registry::get`), so the grep-removal criterion and 'reuse existing verb = RON-only' both hold; only adding a genuinely new param SHAPE for a novel motion is a small core change. The registry seam can evolve additively to a Reflect-erased param representation (option B) later without a rewrite. | Lowest-risk MVP for one Digimon: fully Reflect-able (D034 editor-ready), compiler-validated, deterministic (R004), and the dispatch stays open per D033 (a `match` on verb id never enters core — only the param vocabulary is closed data). All five S02 Baby Flame effects reuse a fixed param-shape set, so S02 never exercises the novel-new-param edge that A trades away. Research (S02-RESEARCH Key Design Tension) explicitly recommends A as the MVP and notes D033 designed A and B as additive. | yes — additively evolvable to Reflect-erased params (option B) if a novel motion ever needs an out-of-core param shape | agent |
| D036 | M004/S02 planning — adding the placement extension axis | architecture | Where the PlacementExt axis and its context/param types live, given the axis must sit on combat's ExtRegistries (D031/D033) but params are serialized with the animation VFX schema (D034) | `PlacementExt` axis + `pub placements: Registry<PlacementExt>` field added to `ExtRegistries` in src/combat/runtime/registry.rs; the pure context `PlacementCtx` and serialized `PlacementParams`/`PlacementAnchor` types are defined in src/animation/vfx_asset.rs (with the schema), and registry.rs imports them for the `PlacementExt::Fn = fn(&PlacementCtx, &PlacementParams) -> [f32;2]` signature. This introduces one new narrow combat/runtime → animation type-import edge. No dependency cycle results (animation has zero combat imports and none is added). | Reusing the existing Registry<E>/ExtRegistries substrate is the milestone's explicit intent (D031/D033, MEM068); CueExt already makes that registry presentation-facing. Serialized verb params belong with the editor-ready schema (D034 cohesion). The edge is a thin type reference for one Fn signature; verified neither module imports the other today, so no cycle. | yes | agent |
| D037 | M004/S05 planning | rendering | S05 rendering acceptance scope for additive VFX | Deliver HDR camera bloom and overbright data-driven VFX colors in S05, while explicitly deferring strict custom additive particle material to a future isolated rendering refactor unless milestone validation later requires it. | Bevy 0.18's built-in 2D sprite/ColorMaterial path does not expose true additive blending, so honest additive delivery would require a custom Material2d plus particle mesh/material conversion that is higher risk than the S05 acceptance gap. HDR bloom plus overbright VFX colors exercises the current data-driven seam and leaves S06 human visual signoff intact without falsely claiming strict additive material support. | Yes | agent |
| D038 | M005 planning; user judged current VFX inadequate and proposed bevy_enoki | library | VFX rendering backend for M005 (and the path forward off the placeholder flat-quad system) | Adopt bevy_enoki (0.6, Bevy 0.18-compatible) as the windowed-gated GPU 2D particle backend, authoring effects in .particle.ron; prove on one Agumon effect (M005/S04 spike) before migrating all Agumon VFX (S05). enoki effects are K001-visual-only; the FSM cue/barrier impact release (D031/D032) stays the deterministic gameplay-timing seam, independent of the particle backend. | The existing one-shot flat-quad spawner with closed-form motion cannot produce emission-over-time, real additive blending, or per-particle animation, so VFX read as placeholder (same root cause as M004/D037). enoki provides those natively. Spiking one effect first retires the new-dependency integration and quality risk before the expensive full reauthoring. VFX is already outside the deterministic test boundary and K001-gated, so this does not break any existing CI contract. | yes — if enoki proves a poor fit at the S04 spike, the milestone falls back to the existing vfx.ron quad path and the migration sketch (S05) is rescoped | collaborative |
| D039 | S05 may revisit the seam shape when migrating all three Agumon skills to enoki; the one-id branch generalizes to a registry/lookup of enoki effect handles. | architecture | How bevy_enoki is wired into the windowed render path for the S04 spike (one effect) without leaking lifetime into the kernel/FSM timeline | Intercept inside the existing `spawn_effect_by_id` seam (src/windowed/render.rs): for the single `baby_flame.impact` id, reuse `anchor_base_xy` to compute the world anchor, then spawn an enoki one-shot `(ParticleSpawner, ParticleEffectHandle, OneShot, Transform)` bundle and return early; all other effect ids keep the quad path. The enoki effect handle lives in an `AgumonEnokiVfx` resource loaded at Startup with a fail-loud `LoadState::Failed` diagnostic. `EnokiPlugin` is registered windowed-gated in `RenderPlugin::build`; the dep is `optional` and reachable only via the `windowed` feature. | Intercepting inside the single seam covers all three call sites (on-enter node, on_expire chain, detonate) uniformly while touching one id only. The enoki spawn is presentation-only and fire-and-forget (OneShot), so it cannot leak particle lifetime into the kernel timeline — the FSM cue/barrier control flow (request_release/fire_kernel_cue, D031/D032) stays untouched. Gating the dep behind `windowed` (mirroring `dep:bevy_egui`) keeps the entire render stack out of the headless build (R002/R005/R016), proven by a dedicated dep-gating test. | yes | agent |
| D040 | M005/S05 planning, post-S04 spike; resolving the soft scope hint on effect count and texture-vs-procedural choice against real render.rs code | architecture | Which Agumon effects migrate to enoki in S05, and how the multi-effect chains collapse | Migrate the three fire-and-forget contact bursts to enoki one-shot Particle2dEffect assets: sharp_claws.slash, baby_flame.impact (enriched to fold the impact_flash radiating shards), and baby_burner.detonate (folding baby_burner.flash into one richer burst). Generalize the spawn_effect_by_id seam from a single hardcoded id branch to a per-effect-id handle map (AgumonEnokiVfx.handles: HashMap). Defer the FSM/cue-timing-coupled mouth buildup (baby_flame.charge/ember) and the cross-gap traveling projectile (baby_flame.projectile) — they remain on the quad path this milestone. | One-shot bursts are enoki's sweet spot and are D031/D032-safe: fire-and-forget with OneShot::Despawn, no particle lifetime enters the kernel/FSM timeline. The charge buildup (emit-until-cue-release) and the projectile (spawn-at-A-travel-to-B with closed-form arc math, then chain impact) are timing-coupled and moving; expressing them in static .particle.ron would require new stateful emitter systems that re-position/despawn enoki spawners each tick, pushing presentation logic toward the kernel-timing boundary the decision explicitly protects. The three contact bursts are the per-skill 'money shots' that most read as placeholder, so each of Sharp Claws, Baby Flame, and Baby Burner gets its primary impact rendered through enoki, satisfying the slice demo at the contact moment. | yes — if K001 sign-off requests the charge buildup and traveling projectile also move to enoki, that is a follow-up slice (stateful emitter systems); the handle-map seam already accommodates adding more effect ids | agent |
| D041 | M005/S05 planning; resolving the soft scope hint on old-quad-path disposition | architecture | Disposition of the windowed quad VFX path once Agumon contact bursts route through enoki | Keep the windowed quad path (vfx.ron VfxAsset + advance_vfx_particles + the quad spawn loop in spawn_effect_by_id) behind the seam as the fallback for any effect id without an enoki handle. Do NOT delete it in S05. Routing is by handle-map presence: an id with an enoki handle spawns the one-shot and returns early; every other id falls through to the unchanged quad loop. | Reversibility — D038's enoki adoption is revisable until K001 visual sign-off. Keying the route on handle-map presence makes rejecting the enoki look a one-line revert (drop the handles) with zero rendering regression and no churn to the quad path. Deleting the now-dormant Agumon quad rendering is a clean-up follow-up gated on K001 approval. The deterministic SpawnParticle to VfxSpawnDescriptor seam (R012/R016/D031/D032) is untouched regardless of which backend renders. | yes — once K001 approves the enoki look, a follow-up can delete the dormant Agumon quad path | agent |
| D042 | M006 planning — extension-first refactor of the windowed presentation layer | architecture | Where per-digimon presentation code lives relative to combat logic | Keep two separate per-digimon homes: combat logic stays in src/combat/blueprints/<digimon>/ (lib crate), presentation stays in src/windowed/digimon/<digimon>/ (binary crate). NOT a unified root-level src/digimon/ module. | User rejected merging logic+presentation into one root module. The windowed presentation lives in the binary crate and is intentionally kept there; integration tests link only against the lib, so windowed per-digimon modules are proven structurally (grep/diff) + K001 manual, not by integration tests. Splitting by concern (logic=lib/testable, presentation=binary/manual) preserves the existing crate boundary. | yes | human |
| D043 | M006 planning — VFX consolidation onto enoki | architecture | Fate of the custom quad VFX system now that bevy_enoki is integrated | Retire the custom quad VFX system (VfxAsset/VfxParticle/resolve_effect/advance_vfx_particles) entirely. bevy_enoki becomes the sole particle VFX renderer for all effects (charge/ember/projectile/impact), not just the 3 contact bursts. | Enoki has strictly more simulation capability than the quad system (velocity/gravity/damping/emission-shape, which the quad system lacks). The only thing the quad system provided that enoki cannot is headless intent-proxy testability (D037). User accepted dropping that: VFX verification becomes K001-manual only. Supersedes D037's rationale for keeping a custom material/quad path. | yes | human |
| D044 | M006 planning — generic cue layer replacing ad-hoc hit_feedback | architecture | How cosmetic feedback (flash, target shake, camera shake, particle burst) is modeled | Unify all cosmetic feedback as data-driven cue primitives addressed by string id in a CueRegistry, following the UE5 GAS GameplayCue model. Primitives: Flash/Blink, SpriteShake (target), CameraShake, ParticleBurst — each parametrized by data (peak/amp/freq/ticks/curve), no hardcoded consts. Cues are fire-and-forget, triggered by CombatEvent or animgraph on_enter, fully decoupled from combat logic. | Today flash and shake are a third ad-hoc system with zero flexibility (consts inline in hit_feedback.rs, coupled to AgumonSprite) and camera-shake is not expressible at all. GAS treats camera-shake as just another cue handler with a different target — same mechanic as particles. Modeling flash/blink and target-shake as cues too (user confirmed) makes camera-shake a non-special case and removes Agumon specificity. Pure cue decay math lives in the lib (headless-testable); dispatch lives in windowed. | yes | collaborative |
| D045 | M006 planning — acceptance gate definition | architecture | What proves the extension-first refactor actually removed hardcoding | Registering a second digimon (Renamon) — both its combat logic and its windowed presentation — entirely via its own modules with ZERO edits to any core/engine file is the milestone acceptance gate. Proven structurally (grep/diff showing no engine edits) plus K001 manual visual sign-off. | A refactor that only moves Agumon code around can still leave hidden hardcoded seams. The only real proof that the presentation layer is extension-first is adding a new digimon without touching the engine. Renamon already has anim_graph.ron + clip.ron assets, minimizing new asset authoring. | no | human |
| D046 | M006/S01 planning — making enoki the sole VFX renderer and deleting the quad system | architecture | How charge/ember mouth-buildup and the traveling projectile→impact chain are reproduced once enoki is the sole renderer (the stateful emitter systems D040 explicitly deferred to the quad path) | Add a thin presentation-side enoki lifecycle layer in src/windowed/render.rs, running on the existing PendingAnimationTicks clock (the slot advance_vfx_particles occupied), fully decoupled from the kernel/FSM timeline (D031/D032 preserved). Two mechanisms: (1) a ChargeEmberEnokiMarker component inserted by spawn_effect_by_id for the charge/ember effect ids (spawned as persistent emitters, NOT OneShot::Despawn) and despawned by marker-query at cue release — replacing the old VfxParticle effect-id despawn at render.rs ~1083-1092; (2) a ProjectileFlight component + per-anim-tick system that lerps the enoki spawner Transform caster→target and, on arrival, despawns the projectile spawner and fires the baby_flame.impact enoki one-shot — replacing the quad ArcLaunch motion and the on_expire projectile→impact chain in advance_vfx_particles (~1877-1891). The three contact bursts (impact/detonate/slash) keep OneShot::Despawn. impact_flash and baby_burner.flash chains stay folded into the impact/detonate .particle.ron (no separate spawn). Enoki spawn placement is migrated off VfxAsset by carrying the per-id anchor in the AgumonEnokiVfx map, so the windowed vfx.ron loader (AgumonVfx) can be deleted while the lib VfxAsset/resolve_effect survive for headless + data-contract tests. | D043 makes enoki the sole renderer for ALL effects, reversing D040 which kept charge/ember/projectile on the quad path precisely because they need stateful per-tick emitter systems (emit-until-release, spawn-at-A-travel-to-B-then-chain). Those systems must now exist enoki-native. A marker component + a flight component are the minimal thin replacements: fire-and-forget, presentation-only, on the animation clock, so no particle lifetime enters the kernel timeline (D031/D032 untouched). Carrying the anchor in the handle map (rather than re-reading vfx.ron) lets the windowed quad data path be fully deleted without disturbing the lib VfxAsset/resolve_effect that headless tests and vfx_asset_impact_render depend on. VFX visual quality is K001-manual per D043; auto-mode verifies via source-contract tests (MEM101) + build/test green. | yes — if K001 sign-off rejects the lerp-based projectile travel, the ProjectileFlight system can adopt arc/easing math without changing the seam; the lifecycle layer is self-contained | agent |
