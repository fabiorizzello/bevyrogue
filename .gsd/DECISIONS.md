# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | M021 — kernel-combat / blueprint state mutation | Canonical write-path per la mutazione di state Digimon-specifico (blueprint state HashMap (UnitId, &'static str) → i32) durante una cast | **`Intent::SetBlueprintState { unit, key, value }` come unico canale di mutazione.** Le hook fn leggono `ctx.blueprint_state(unit, key) -> i32` (default 0) e scrivono esclusivamente via `ctx.enqueue(Intent::SetBlueprintState { ... })`. Nessuna mutazione diretta del HashMap dalle fn. Il payload v0 è `value: i32` semplice (non varianti tipizzate per key) — sufficiente per il roster (Predator Loop counter, Twin Core heat stack, Battery Loop charge). Validato dallo spike pass-3 (Predator Loop). Aggregator centralizzato lato `intent_applier` (D008 double-layer): la mutation finisce sia nel transition stream osservabile sia nel HashMap per la prossima read. | Read via accessor + write via Intent è il pattern già consolidato per ogni altra mutazione (damage, heal, status). Mutare il HashMap direttamente dalle hook fn romperebbe l'invariante "tutte le mutazioni passano dal transition stream" (D008), rendendo non-replicabile lo stato per replay/forensics. `i32` generico evita di forzare le hook a conoscere uno schema chiuso di key (anti-coupling kernel ↔ blueprint, P001). Spike F12 valida che read-via-predicate + write-via-Intent compongono naturalmente: il predicate `agumon::predator_active` legge il counter, l'on_kill hook scrive l'increment. Costo: due righe per hook che muta state, in cambio di full observability nel JSONL. Alternativa "key-tipizzate (enum)" rifiutata perché ricrea coupling P001 elimina; alternativa "mutazione diretta" rifiutata perché perde observability. | Sì; aggiungere varianti `Intent::IncBlueprintState`, `Intent::AddBlueprintState`, etc. è additivo se il pattern `Read+Write` per increment diventa ripetitivo. Sostituire `i32` con `enum Value { Int(i32), Bool(bool), ... }` è additivo. Promuovere a key-tipizzate è breaking. | collaborative |
| D002 | M021 post-spike pass 2/3 — superseding di D007 e D012 per riflettere il design fn-by-id validato | M021 — kernel-combat / blueprint structure | Lifecycle del `BlueprintRegistry` (Resource HashMap owner → Box&lt;dyn Blueprint&gt;) come definito in D007 — ancora load-bearing post spike pass 2/3? | **D007 superseded de facto da D032.** Lo spike pass 2/3 ha rimosso il `trait Blueprint` e il `BlueprintRegistry: Resource&lt;HashMap&lt;owner, Box&lt;dyn Blueprint&gt;&gt;&gt;` dal critical path. Sostituito da: ogni Digimon è un modulo `src/combat/blueprints/&lt;x&gt;/` con una fn `register(reg: &mut ExtRegistries)` che installa hook/predicate/selector/formula/tick/ai/cue/timeline sulle 7 registry parametriche (D031 `Registry&lt;E: ExtPoint&gt;`). Nessuna struct `Box&lt;dyn Blueprint&gt;` allocata, nessun `BlueprintRegistry` separato. `ExtRegistries` è il singolo `Resource` Bevy registrato a startup. La domanda "startup-frozen vs single-writer mutable" di D007 non si applica più — `ExtRegistries` è frozen by construction (registrazione una sola volta in `App::build`, mai mutata dopo). Hot-reload/DLC dinamico restano deferred come da D007. | D007 cristallizzava la forma "trait Blueprint + Box&lt;dyn&gt; registry". Lo spike ha mostrato che il pattern fn-by-id (D023 hook fn + D030 selector fn + D031 Registry&lt;E&gt;) è strutturalmente più semplice e copre lo stesso set di responsabilità senza trait objects, allocazioni, o registry separato. Le risposte di D007 sul lifecycle (startup-frozen, `pub(crate) register`, no hot-reload) restano corrette ma applicate al nuovo container (`ExtRegistries`). Formalizzo la supersessione qui per audit-trail invece di editare D007 in-place (rispetta append-only del DECISIONS.md). D012 (type-level enforcement blueprint↔state via `register_blueprint::&lt;B: Blueprint&gt;`) decade conseguentemente — sostituito da: ogni `register()` per-Digimon installa direttamente `app.register_blueprint_state::&lt;State&gt;()` se serve state mutabile (D034 ne è il write-path). | No, è una supersessione definitiva: ripristinare `trait Blueprint + Box&lt;dyn&gt;` post-spike richiederebbe evidence che il pattern fn-by-id ha un limite reale (nessuno emerso in 33/33 test verde sui 4 pattern stress-testati). Hot-reload futuro è additivo su `ExtRegistries` (Resource mutabile + invalidation) come era additivo su `BlueprintRegistry`. | collaborative |
| D003 | M021 S01 T02 — wiring intent_applier for DealDamage canary | architecture | System signature for `intent_applier` — exclusive `&mut World` vs Bevy query params | `intent_applier` is an exclusive system (`fn intent_applier(world: &mut World)`) that snapshots source/target unit data without Bevy query aliasing | Reading source and target unit components simultaneously via normal queries would require ParamSet or multiple separate queries, adding complexity. Exclusive world access lets us snapshot exactly what we need (HP, toughness, etc.) for the damage formula in one clear pass without aliasing errors, at the cost of scheduler exclusivity which is acceptable since intent_applier runs at a fixed point in the turn pipeline. | Yes; if intent_applier grows to multi-entity batch resolution, a SystemParam approach with explicit component access sets may perform better and allow parallel scheduling. | agent |
| D004 | M021 S01 T04 — CombatPlugin extraction, resource initialization | architecture | Canonical seed for `CombatRng` inserted by `CombatPlugin` | `insert_resource(CombatRng::from_seed(0xDEAD_BEEF))` as the deterministic default seed in CombatPlugin::build | Tests require deterministic RNG; a fixed memorable sentinel (0xDEAD_BEEF) makes the default seed visible and overridable. headless.rs already called `init_resource` for CombatRng which becomes a no-op after the plugin runs (Bevy init_resource skips if resource already present), preserving backward compat. Callers that need a different seed can insert_resource before adding the plugin. | Yes; production should accept seed from config/CLI rather than hardcoded sentinel. Migration: add a CombatPluginConfig { seed: u64 } parameter to CombatPlugin before S05. | agent |
| D005 | M021 S03 planning — resolving Decision A from S03-RESEARCH.md | architecture — M021/S03 kernel clock boundary (D026) | Does S03 implement the full Windowed runtime, or only the two-clock invariant? (S02-SUMMARY deferred AdvanceMode to S04, but S03 roadmap demands "two-clock verde".) | Split the concern. S03 owns the INVARIANT only: BeatRunner gains a `clock: Clock` field (defaults to HeadlessAuto in `new`, set via a `with_clock`/setter builder so existing S02 call sites are untouched). Under `Clock::Windowed`, a beat carrying `Presentation` fires its hook/intents normally, then `step` returns a new `StepOutcome::AwaitingCue` WITHOUT advancing the cursor (one-shot latch per beat); `resume_cue()` clears the latch so the next `step` advances. `run_to_completion` auto-resumes on `AwaitingCue` so S02 drive-to-completion tests still terminate. HeadlessAuto path is byte-identical to today (no AwaitingCue ever emitted). S04 owns the FULL Windowed runtime (SignalBus-driven cue completion, PassiveRunner, real animation signal). | The invariant (identical Intent stream across clocks; only timing differs) is provable with a minimal latch that does not require SignalBus or the animation engine. Firing the beat before stalling guarantees the Intent stream is clock-independent by construction. Keeping `BeatRunner::new` signature stable and adding the clock via a builder avoids a breaking change across the three green S02 integration tests and the inline runner unit tests — only `run_to_completion`'s exhaustive `StepOutcome` match needs the new arm. This honors the S02→S04 deferral while satisfying the S03 roadmap line. | Yes — S04 replaces the latch with a SignalBus-driven cue-completion signal; AwaitingCue/resume_cue are the stable seam S04 builds on, not throwaway. | agent |
| D006 | M021 S03 planning — resolving Decision B from S03-RESEARCH.md | architecture — M021/S03 mode-parity proof scope (D024) | Does S03 enforce DryRun/Preview no-apply semantics (gate intent_applier), or prove Intent-stream parity only? | B1 — S03 proves Intent-STREAM parity only. The branched-chain parity test runs `BeatRunner` 3× over the same world under Execute/DryRun/Preview and asserts the resulting `pending` queues are equal WITHOUT draining them through `intent_applier`. No `applier.rs` change. Stream equality uses a test-local normalization helper `format!("{:?}", intent)` projection (Intent derives Clone but NOT PartialEq, and deriving it is blocked by future-S04 payload changes — a derive is explicitly avoided). | D024's invariant is about the Intent STREAM being mode-independent; "no-apply" is a property of the CONSUMER of the stream, which is C1/S11's concern (UI/AI using DryRun without applying). Gating intent_applier now would pull S11 scope into S03 with no test that needs it. The runner already never applies (it only fills `pending`), so stream parity is the complete and literal proof of "Test DryRun≡Execute≡Preview verde su chain ramificata". | Yes — S11 (C1) adds the consumer-side no-apply gate on intent_applier; that is additive and does not invalidate the stream-parity proof. | agent |
| D007 | M021 S03 planning — resolving Decision C from S03-RESEARCH.md | architecture — M021/S03 branching mechanism / SelectorCtx scope | Does the "chain ramificata" (branched chain) parity fixture require world-aware SelectorCtx, per the S02 follow-up "S03 must wire real world-aware selectors"? | No. Branching in S03 fixtures is achieved via edge-gate PREDICATES, which already receive a `SkillCtx` carrying `world: &World` and can read live HP/team now. `SelectorCtx` world-access stays DEFERRED beyond S03 (it remains `state: &()`); no `timeline.rs`/`SelectorCtx` change in S03. Fixtures use a hard-coded selector (like the S02 chain_bolt port) plus a world-reading edge predicate to route the branch. | The S03 roadmap deliverable is the parity/two-clock/circuit-breaker invariants, not target-selection realism. Edge-gate predicates already have world access, so a genuinely branched timeline (predicate reads target HP → routes to a different beat) is provable without touching SelectorCtx — the single largest optional lift. Wiring world into SelectorCtx is target-selection realism, properly owned by a later slice (S06+ migration), and keeping it out preserves the timeline.rs Bevy-free discipline established in S02. | Yes — a later slice that needs world-driven target SELECTION (not branching) wires concrete `S` into SelectorCtx mirroring how SkillCtx borrows &World; this deferral does not block that. | agent |
| D008 | M021/S05 planning | architecture | How asset-loaded CompiledTimeline identifiers should be stored in the runtime model | Move CompiledTimeline/Beat/Presentation reference fields from &'static str to owned string storage so RON-compiled skills can populate timelines without leaks, while ExtRegistries remain keyed by &'static str and continue to resolve lookups by &str. | skills.ron data cannot safely produce &'static identifiers without leaking or pinning reload state. Owned timeline IDs keep asset compilation reload-safe while preserving the frozen startup registry contract and O(1) registry lookups. | Yes | agent |
| D009 | M021/S05 planning | architecture | How generic built-in hooks receive per-beat effect parameters from RON timelines | Add typed per-beat payload data to Beat so built-in hooks can interpret damage, toughness, status, delay, buff, and blueprint-signal parameters directly from the compiled timeline instead of encoding values into hook IDs. | A plain hook ID cannot express canonical skill parameters like damage amount or status duration. Beat-local payloads preserve fn-by-id registries, keep validation tractable, and avoid exploding the registry with one hook per concrete skill value. | Yes | agent |
| D010 | M021/S05 planning | runtime | Where strict validation for asset-backed RON timelines should run | Compile and validate SkillBook timelines when the SkillBook asset loads, mirror the validated results into TimelineLibrary, and panic immediately on compile or dangling-reference errors; CombatPlugin::finish remains responsible for statically registered timelines present before finish. | Bevy asset loading happens after plugin finish, so App::finish alone cannot see skills.ron timelines. Load-time compilation keeps typo detection strict at boot/startup without requiring a synchronous asset preload path. | Yes | agent |
| D011 | M021 / S05 / T01 | architecture | Timeline-backed combat assets use owned timeline identifiers while existing hand-built runtime tests keep the borrowed default type parameter, and the kernel built-in extension functions are registered during CombatPlugin setup. | `CompiledTimeline<Id = &'static str>`/`Beat<Id>` remain default-borrowed for current code, while `TimelineLibrary<String>` holds asset-backed compiled timelines and `register_kernel_builtins` runs in `CombatPlugin::build`. | This preserves all existing hand-authored tests and runtime code without string leaking, while still making the asset compiler path own its ids and ensuring built-ins are available before timeline validation at startup. | Yes | agent |
| D012 | M021/S05/T02 | architecture | How SkillBook timeline-backed skills are loaded and validated | Add an optional SkillTimeline schema to SkillDef, compile those timelines on SkillBook asset load into TimelineLibrary<String>, and panic immediately on missing registry refs or structural timeline errors with skill + beat/edge context. | This keeps legacy skills legal during incremental migration while ensuring RON typos fail before gameplay runtime and the compiled kernel timeline path has a single shared library to consume. | Yes | agent |
| D013 | M021/S06 planning | architecture | How S06 should establish real loop coverage while removing the legacy Effect path | Use the shipped `petit_thunder` asset as the first real loop-backed canon migration, then batch the remaining child-roster active skills onto timelines before deleting `Effect`-derived runtime dispatch and schema support. Passive/form-identity-only behavior remains for later slices. | Research showed loop/bounce is the only unproven active runtime shape, and deleting `Effect` early would create a wide half-migrated state. Aligning `petit_thunder` with its documented bounce canon gives the slice a real asset-backed loop proof, while sequencing batch migration before resolver removal preserves boot-time validation quality and keeps the repo runnable through the transition. | Yes | agent |
