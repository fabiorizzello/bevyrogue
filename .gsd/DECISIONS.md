# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M021/S07/T04 | combat | Emit `BlockReactionTriggered` whenever a one-shot passive mitigation modifier is consumed in the incoming-damage pipeline, not only when Tentomon's reactive block proc succeeds. | Use the shared event as the canonical diagnostic surface for any pre-damage passive mitigation, with Tentomon's block reaction and generic passive damage modifiers both funneled through the same event. | This keeps the damage pipeline and the Tentomon passive aligned, preserves the existing generic block-reaction test, and gives one observable seam for future passive mitigation effects. | Yes | agent |
| D002 |  | architecture | How TwinCore transitions route through CombatKernelTransition after removing the digimon-specific variant | Replace `CombatKernelTransition::TwinCore(TwinCoreTransition)` with `CombatKernelTransition::Blueprint { owner: "twin_core", name: "<signal_name>", payload: SignalPayload::Amount(amount) }`. The `apply_twin_core_transitions_system` decodes Blueprint events where owner=="twin_core" and maps name to the corresponding TwinCoreSignal internally within the twin_core module. | M021 CONTEXT M5 mandates removal of 5 digimon-specific variants from CombatKernelTransition, replaced by a generic Blueprint variant. TwinCore is shared between Agumon and Gabumon but is still a blueprint-level mechanic, not a kernel primitive. Routing through Blueprint { owner: "twin_core" } keeps the kernel generic while preserving full observability via JSONL (same event shape as passive triggers from S07). | Yes | agent |
| D003 | M021/S10 planning | combat | How shared event and observability surfaces should represent digimon-specific runtime resolution after S10 removes digimon names from shared kernel files | Remove digimon-named resolved variants from shared CombatEventKind and shared observability structs; use owner-gated blueprint runtime state/snapshots as the canonical diagnostic seam, with shared surfaces exposing only generic Blueprint envelopes and generic formatting/aggregation. | S10's exit proof requires src/combat outside blueprints to be digimon-free, which conflicts with keeping BatteryLoopResolved, PredatorLoopResolved, holy_support, and precision_mind_game named seams in events.rs and observability.rs. Blueprint-owned state already carries the mechanic-specific detail, so moving the diagnostic contract behind owner-owned resources preserves determinism while allowing shared kernel and observability code to stay generic. | Yes | agent |
| D004 | M021/S11 planning | architecture | How S11 world-backed preview consumers should integrate with existing UI and enemy-turn systems | Keep `query_skill_preview` as a shared world-backed helper, then consume it through narrow exclusive bridge systems/resources (UI preview cache and enemy-AI decision bridge) instead of converting `combat_panel` or `advance_turn_system` wholesale into exclusive systems. | Both consumers need `&mut World` to run `BeatRunner` in `SkillCtxMode::Preview`, but the current egui panel and turn-advance systems are typed Bevy systems with broad existing call sites and tests. Narrow bridge systems preserve headless-first structure, minimize schedule churn, and let UI/AI share the same preview contract without duplicating timeline execution logic. | Yes | agent |
| D005 | M021/S12 planning | architecture | How S12 should remove digimon-named roster metadata and validation snapshot fields | Adopt a generic owner-keyed `UnitDef` blueprint payload plus a new `ValidationExt` registry axis, and keep S12 scoped to schema/validation decoupling rather than broader plugin-registration or asset-layout automation. | Research and code validation show the current hardcoded `twin_core`/`holy_support` roster fields and named validation snapshot substructures are the load-bearing shared seams for S12, while the stronger 'new digimon touches only 2 dirs' proof is additionally blocked by central blueprint registration and the monolithic `assets/data/units.ron` loader. A generic owner-keyed payload and registry-driven validation contributors satisfy milestone context C2/C3 without introducing another shared per-digimon enum, and they keep deterministic serialization/formatting feasible. | Yes | agent |
| D006 | M021 fascia B — apertura milestone, post-research §8 Q-B2 | M021 — kernel-combat / blueprint extensibility | Type of the `Intent::BlueprintSignal` payload — opaque vs closed enum | Keep `Intent::BlueprintSignal` payloads opaque to the kernel with owner-scoped downcast helpers inside the owning mechanic/plugin. | A closed blueprint-side enum would recreate the coupling M021 is trying to remove. Opaque owner-scoped payloads keep the kernel generic while each shared mechanic owns its concrete transition type. | Yes | collaborative |
| D007 | M021 fascia B — apertura milestone, post-research §8 Q-B3 | M021 — kernel-combat / blueprint extensibility | Lifecycle of the BlueprintRegistry / blueprint registration container | Use a startup-frozen registry/resource for blueprint registration, with registration only during startup and no hot-reload path in M021. | M021 does not need dynamic registration, and single-writer mutable hot-reload would add invalidation complexity for little value. This was later superseded in implementation by the fn-by-id `register()` pattern, but it is still part of the historical decision trail. | Yes | collaborative |
| D008 | M021 pre-roadmap — Q7 settled | M021 — kernel-combat / observability | How BlueprintSignal enters the transition stream | Route `Intent::BlueprintSignal` through the kernel transition stream first, then dispatch to the owning runtime/mechanic. | If blueprint state changes bypass the transition stream, they disappear from JSONL and replay/forensics become much harder. The extra hop preserves the invariant that externally relevant state mutations stay observable. | Yes | collaborative |
| D009 | M021 pre-S01 | M021 — kernel-combat / skill API | Historical D023 — Shape of chain hooks (`on_cast` / `on_hit` / `on_kill` / `on_miss` / `on_final_hop`) | Use Rust hook functions registered by ID instead of a declarative OnHit/OnKill enum surface. | Function-by-ID hooks eliminate enum bloat, keep the kernel closed to extension, and unify chain hooks with the passive/listener model. The observable contract stays the emitted Intent and CombatEvent stream, not the hook body. | No | collaborative |
| D010 | M021 pre-S01 | M021 — kernel-combat / skill API + UI/AI | Historical D024 — Dry-run/preview support for hook functions | Expose an explicit `SkillCtx::Mode { Live, DryRun }` so the same resolve path powers execution and preview safely. | Without an explicit dry-run mode, preview would either mutate live queues or require a duplicated prediction path. A mode switch keeps `preview ≡ execute` structurally true while containing side effects. | Yes | collaborative |
| D011 | M021 pre-S01 | M021 — kernel-combat / skill execution model | Historical D025 — Explicit timeline FSM vs monolithic `apply_effects()` | Make the timeline FSM the load-bearing execution model: skills compile to beats and edges, and a deterministic BeatRunner advances them beat-by-beat. | A monolithic effect matcher forces every new mechanic to extend kernel enums and match ladders. The FSM keeps structure in data and semantics in shared runtime code, which is exactly the decoupling M021 is trying to achieve. | No | collaborative |
| D012 | M021 pre-S01 | M021 — kernel-combat / timing model | Historical D026 — Deterministic kernel clock vs windowed presentation clock | Use a two-clock model: headless execution resolves immediately, while windowed mode attaches presentation timing, but the emitted Intent stream stays identical. | If UI timing alters the emitted runtime stream, replay and tests diverge from windowed behavior. Two clocks preserve determinism while still allowing presentation pacing in the UI layer. | Yes | collaborative |
| D013 | M021 pre-S01 | M021 — kernel-combat / skill mutation | Historical D027 — Demote `TimelinePatchOp` in favor of runtime edge gating | Do not use compile-time timeline patch operations for talents by default; keep one canonical timeline and gate optional branches with runtime predicates. | Compile-time patching duplicates representations of the same skill and makes gating harder to observe. Runtime predicates keep one canonical timeline and make talent-dependent paths visible and testable. | Yes | collaborative |
| D014 | M021 pre-S01 | M021 — kernel-combat / signal bus | Historical D028 — Closed signal bus vocabulary | Keep the signal vocabulary closed and validated: use a fixed signal type/taxonomy rather than free-form strings as the shared kernel bus. | Open string-based buses invite typo-driven failures that are hard to debug. A closed signal surface preserves compile-time or startup-time validation while leaving owner-specific extensibility to blueprint-owned payloads. | Yes | collaborative |
| D015 | M021 pre-S01 | M021 — kernel-combat / FSM dispatch | Historical D029 — Beat edge dispatch semantics | Evaluate outgoing beat edges in declaration order and take the first passing edge, with a required final unconditional fallback edge. | First-passing edges make branch precedence explicit without forcing mutually exclusive predicates everywhere. A required fallback edge prevents stuck timelines when every conditional edge fails. | No | collaborative |
| D016 | M021 pre-S01 | M021 — kernel-combat / target selection | Historical D030 — Target selector shape: enum primitives plus selector fn-by-id | Keep primitive target shapes in the core model, but register all smart/custom selectors by ID in the selector registry. | Custom selector logic like bounce, adjacency, and owner-specific targeting should not expand kernel enums. Registering selectors by ID keeps the kernel stable while letting blueprints own targeting behavior. | No | collaborative |
| D017 | M021 pre-S01 | M021 — kernel-combat / extensibility framework | Historical D031 — Unified fn-by-id extensibility registries | Use one generic `Registry<E>` pattern across extension axes and group them under a single `ExtRegistries` resource. | A single registry pattern avoids seven drifting implementations of the same idea and keeps registration/discovery uniform. Function pointers are enough here; trait objects and embedded DSLs add complexity without value. | Yes | collaborative |
| D018 | M021 pre-S01 | M021 — kernel-combat / blueprint structure | Historical D032 — Blueprint module plus `register()` function | Represent each Digimon as a module with `register(reg: &mut ExtRegistries)` rather than a trait-object blueprint instance. | The module-plus-register pattern keeps blueprint wiring static, simple, and allocation-free. It matches the current fn-by-id design better than a `trait Blueprint + Box<dyn Blueprint>` container. | No | collaborative |
| D019 | M021 pre-S01 | M021 — kernel-combat / runtime context | Historical D033 — Immutable skilltree plus native Loop beats and `hop_index` | Keep the skilltree immutable during a run, model bounce/repeat behavior with native Loop beats, and carry `hop_index` on BeatEvent. | Immutable skilltrees preserve replay determinism, while native Loop beats capture repeated-hit patterns without reintroducing kernel special cases. Keeping `hop_index` on BeatEvent preserves one predicate signature instead of splitting the registry surface. | Yes | collaborative |
| D020 | M021 post spike pass-3 | M021 — kernel-combat / blueprint state mutation | Historical D034 — Canonical mutation path for blueprint state | Use `Intent::SetBlueprintState` as the canonical write path for blueprint-owned mutable state. | Direct state mutation from hooks would bypass the transition/event stream and break replayability and observability. Writing through Intent keeps blueprint-state changes aligned with the rest of the kernel mutation model. | Yes | collaborative |
| D021 | M021 post spike pass-2/3 | M021 — kernel-combat / blueprint structure | Historical D035 — Supersede the trait-object BlueprintRegistry design | Treat the old `BlueprintRegistry<Box<dyn Blueprint>>` design as superseded by the fn-by-id `register()` plus `ExtRegistries` architecture. | The spike validated that fn-by-id registration covers the same responsibilities with less indirection and less runtime machinery. Recording this supersession preserves why the project no longer follows the earlier trait-object plan. | No | collaborative |
